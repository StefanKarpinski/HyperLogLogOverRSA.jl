var documenterSearchIndex = {"docs":
[{"category":"section","location":"06-protocol-summary/#Protocol-Summary","page":"Protocol Summary","text":"Here we summarize the full HyperLogLog Over RSA protocol for anonymously estimating the number of unique clients making various kinds of requests to a server or set of servers. At a high level:\n\nClients and servers agree on protocol parameters.\nThe server generates a HyperLogLog RSA ring and publishes it, along with a semigenerator and a certificate that the modulus is fingerprint-free.\nThe client downloads the ring certificate and checks that the parameters are acceptable and that the certificate is valid.\nThe client generates and stores a master key ring element.\nFor each request, the client generates and sends a freshly randomized encrypted HyperLogLog value, keyed on the master key and the resource class of the request.\nThe server responds to requests and collects request logs, including encrypted HyperLogLog values, which may or may not be valid.\nAfter request logs have been aggregated, HyperLogLog values can be validated and decoded.\nOnce individual HyperLogLog values have been decoded, client count estimation is just a matter of normal HyperLogLog aggregation and estimation.\n\nA test implementation of the entire HyperLogLog Over RSA protocol in Julia can be found here.","title":"Protocol Summary"},{"category":"section","location":"06-protocol-summary/#Server-step-0:-Parameter-selection","page":"Protocol Summary","text":"The system operator chooses HyperLogLog parameters, RSA bit-length, and a certificate strength:\n\nB is the bucket count\nIt must be an odd number\nExample: B = 2^12 - 1\nm is the maximum geometric sample value\nMaximum client estimate is around 2^m\nExample: m = 63\nL is the bit-length of N values\nControls cryptographic strength of the RSA ring\nExample: L = 1024\nalpha is the certificate strength\nEffort to generate a valid-looking malicious N value\nShould be at least 2^lambda for the system security level lambda, and never below 2^80 (see Malicious servers)\nExample: alpha = 2^128","title":"Server step 0: Parameter selection"},{"category":"section","location":"06-protocol-summary/#Client-step-0:-Parameter-acceptance-criteria","page":"Protocol Summary","text":"On behalf of clients, the client implementor should choose acceptance criteria for protocol parameters, including:\n\nB_max — maximum bucket count\nThe simplest way to fingerprint clients is just to choose B = 2^128 and let the bucket be the fingerprint. This limit prevents that kind of “attack”.\nExample: B_max = 2^16\nm_max — maximum geometric sample\nThis is mostly a sanity check since extreme geometric samples are so rare as to not matter. Capping it also keeps the client’s per-request exponent arithmetic within fixed-width integers and prevents a malicious server from forcing a client to compute in some absurdly large geometric range. A cap of 63 already allows cardinality estimates up to around 2^63, astronomically more than any real client population.\nExample: m_max = 63\nL_max — maximum modulus bit-length\nThis is also mostly a sanity check to make sure that clients aren’t DoSed by being made to do arithmetic in some absurdly large modulus.\nExample: L_max = 2^20\nalpha_min — minimum certificate strength\nThis is the least number of modulus values a malicious server would expect to have to try in order to find one that passes certificate checks. The server can provide more square roots than this strength implies, but not fewer.\nExample: alpha_min = 2^80","title":"Client step 0: Parameter acceptance criteria"},{"category":"section","location":"06-protocol-summary/#Server-step-1:-Ring-generation","page":"Protocol Summary","text":"The RSA ring is Z_N where\n\nbeginaligned\nN = PQ = (2 B p + 1)(2^m q + 1)\nendaligned\n\nsuch that P, Q, p, q, are all distinct odd primes coprime to B.\n\nThe server generates N such that:\n\nbeginaligned\n2^L-1  N  2^L\nendaligned\n\nThis can be accomplished by:\n\nChoose random p\nCheck that p is prime\nCheck that P = 2 B p + 1 is prime\nCheck that B notin setP p\nChoose random q\nCheck that q is prime\nCheck that Q = 2^m q + 1 is prime\nCheck that setB P p inter setQ q = emptyset\n\nThe ranges that p and q are chosen from should be computed such that N = PQ falls into 2^L-1 2^L) as required. It’s also desirable for P and Q to have log_2(P) approx L2 approx log_2(Q) for some notion of approximation.","title":"Server step 1: Ring generation"},{"category":"section","location":"06-protocol-summary/#Server-step-2:-Semigenerator-selection","page":"Protocol Summary","text":"The server chooses a random semigenerator, g in Z_N^*.\n\nThis can be accomplished by:\n\nChoose random g in Z_N\nCheck that fmod(g P) is a generator in Z_P\nCheck that fmod(g Q) is a generator in Z_Q","title":"Server step 2: Semigenerator selection"},{"category":"section","location":"06-protocol-summary/#Server-step-3:-Square-root-computation","page":"Protocol Summary","text":"The server computes the number of certificate square roots required based on the target certificate strength, alpha:\n\nbeginaligned\nn = ceilfraclog_2(alpha)log_2(8)-log_2(5) \nendaligned\n\nThe server generates n pairs of values, setx y subseteq J_N^+, using an agreed-upon hashing scheme that includes the value N as a hash input. The hashed values must land in J_N^+ (positive Jacobi symbol): since a hash naturally produces values of either Jacobi symbol, the scheme fixes a twist element tau with Jacobi_N(tau) = -1 — chosen deterministically, e.g. the smallest such value — and multiplies any hashed value of negative Jacobi symbol by tau, which maps it into J_N^+. For each pair, setx y:\n\nIf x is a quadratic residue, add r such that r^2 = x bmod N to the list of square roots\nOtherwise, if y is a quadratic residue, add r such that r^2 = y bmod N to the list of square roots\nOtherwise, if xy is a quadratic residue, add r such that r^2 = xy bmod N to the list of square roots\n\nSince N is a semiprime and x y in J_N^+, one of these three checks must succeed (see Malicious servers), so one square root value is added to the list for each pair generated.","title":"Server step 3: Square root computation"},{"category":"section","location":"06-protocol-summary/#Server-step-4:-Ring-certificate-publication","page":"Protocol Summary","text":"The server publishes a ring certificate containing:\n\nB — the bucket count\nm — the maximum geometric sample\nN — the RSA ring modulus\ng — the ring semigenerator value\ntextsqrts — a list of square roots","title":"Server step 4: Ring certificate publication"},{"category":"section","location":"06-protocol-summary/#Client-step-1:-Ring-certificate-checking","page":"Protocol Summary","text":"The client downloads the latest ring certificate at an agreed upon location. It may or may not have a previously downloaded and verified ring certificate. If it has a downloaded ring and the new one is the same, then it can simply use the existing ring and twist element. If it doesn’t have an existing ring certificate or the new one is different, then the client should delete the old ring certificate and proceed with checking and saving the new certificate and twist element.\n\nTo check a ring certificate, the client should:\n\nCheck that B  B_max\nCheck that B is odd\nCheck that 2  m  m_max\nCheck that log_2(N)  L_max\nCheck that N = 3 bmod 4\nCheck that gcd(B N) = 1\nCheck that gcd(B N-1) = 1\nCheck that Jacobi_N(g) = 1\nCheck that alpha_min  (85)^n where n is the length of textsqrts\nCheck that each square root is valid\n\nIf a certificate passes checks, the client should generate a random twist element:\n\nChoose random x_0 in Z_N\nCheck that Jacobi_N(x_0) = -1\n\nReplace any existing ring record with a new ring record:\n\nB, m, N, g, x_0\n\nThis must be stored persistently so that the client uses the same x_0 in different sessions. The client should check for a new ring certificate periodically.","title":"Client step 1: Ring certificate checking"},{"category":"section","location":"06-protocol-summary/#Client-step-2:-Request-generation","page":"Protocol Summary","text":"When the client needs to send a request in a resource class, it computes:\n\nbeginaligned\nx = x_0 g^h = x_0 g^hash(x_0textclass)\nendaligned\n\nIt then generates a random white noise element and a random exponent value:\n\nChoose z in set1 2 dots N-1 and let w = z^B2^m bmod N\nChoose i in set0 1 dots 2^m-1-1 and let t = 2Bi + 1\n\nFinally, the client computes:\n\nbeginaligned\ny = fmod(wx^t N)\nendaligned\n\nThis is the value it sends along with the request in a header. The request should also include some hash of the ring parameters so that the server can know which ring the value belongs to. This could just be the modulus, or a cryptographic hash of the ring certificate. Note that N will be large enough that a cryptographic hash is actually the more compact option.","title":"Client step 2: Request generation"},{"category":"section","location":"06-protocol-summary/#Server-step-5:-Request-validation-and-decoding","page":"Protocol Summary","text":"The server should not attempt to validate request headers while responding to requests, it should simply serve requests and log the header information for later processing. This also means that the factorization of N does not need to reside on public-facing servers—it only needs to be available for offline log processing. This significantly reduces the chances of the factorization being accidentally leaked or exfiltrated by an attacker.\n\nWhen post-processing request logs, the server can validate the submitted y values by checking that the N value is known and that Jacobi_N(y) = -1. Any request with an unknown ring modulus or that fails the Jacobi symbol test should be ignored for client counting purposes.\n\nThe server decodes the HLL value by computing:\n\nbeginaligned\ntextbucket = fmod(y^2p P) \ntextgeometric = m - log_2(ord(fmod(y^q Q))) \nendaligned\n\nThe bucket value is in Z_P which is huge, but there are only B possible values it takes which can be mapped back to set0 dots B-1 by any consistent mapping scheme. The geometric values are already in set0 dots m and the value can be computed efficiently by repeatedly squaring fmod(y^qQ) until reaching one.","title":"Server step 5: Request validation & decoding"},{"category":"section","location":"06-protocol-summary/#Server-step-6:-Count-estimation","page":"Protocol Summary","text":"Once the bucket and geometric count have been decoded, estimating the unique client count within any set of logs for the same resource class is a matter of doing normal HyperLogLog aggregation and estimation: compute the maximum geometric sample size for each bucket and use the histogram of those per-bucket maxima to estimate the client count. Several good estimators exist. The reference implementation uses the improved estimator of Otmar Ertl, “New Cardinality Estimation Methods for HyperLogLog Sketches”: a closed-form estimator that is effectively unbiased across the whole cardinality range—with no separate small- or large-range corrections—and has relative standard error of about 104sqrtB. The same paper’s maximum likelihood estimator is marginally more accurate (it attains the theoretical variance bound) but requires an iterative solve rather than a closed form. Both supersede the original Flajolet et al. estimator, and both are more principled than the empirical bias-correction tables of HyperLogLog++ (Heule et al.), which is a separate method and not as accurate.\n\nA pleasant feature of this protocol is that once HyperLogLog values are validated and decoded, aggregation and estimation can be done by anyone, so long as they only try to aggregate within resource classes. In other words, the “over RSA” part of the protocol can be forgotten about entirely once HyperLogLog values are decoded. At that point, it’s as if the collection of logs had been recorded with normal HyperLogLog values keyed on client ID and resource class for each record—which is also a reminder of what must not be done with them, the subject of the next section.","title":"Server step 6: Count estimation"},{"category":"section","location":"06-protocol-summary/#Reporting-and-publishing-counts-safely","page":"Protocol Summary","text":"Client count estimates are not of any use unless they get published. HyperLogLog allows estimating the number of unique clients in arbitrary slices of requests, but some care should be taken in what slices are actually reported.\n\nIt is tempting to think that, because each decoded record is only a small HyperLogLog value, the decoded logs are harmless and could simply be published as raw request-level records. They should not be. In any class small enough that clients are unlikely to share (b k) pairs, these decoded pairs uniquely identify clients, in much the same way that being a FreeBSD user effectively uniquely identifies someone (we know what you’re up to, Alex). Publishing decoded HyperLogLog pairs, would also give an attacker an oracle for decoding their HLL values, which, at the very least, would allow them to learn which pairs are rare.\n\nWhat is safe to expose is aggregate output for large enough slices: per-slice cardinality, rather than the per-request (b k) values themselves. This is safe so long as a minimum-cardinality floor is enforced—report no slice whose estimated cardinality falls below a threshold T, folding everything beneath it into a single “other” bucket. How large T must be has a clean answer: a published estimate keeps a client anonymous exactly when it cannot reveal whether that client is present, and we can measure how close it comes. Adding or removing one client shifts the expected estimate by 1, against a standard error of about 104nsqrtB, so a single client’s detectability is the ratio sqrtB(104n). Once that ratio drops well below 1, the estimator’s own sampling noise drowns out any individual contribution. Setting n = sqrtB (approx 64 for B = 2^12) makes that ratio about 1, so at a cardinality of sqrtB a lone client is still fully visible; bringing it down to a quarter or an eighth takes only a small multiple more, a floor of four to eight times sqrtB, a few hundred clients, with a round T = 1024 at the conservative end. The bound is for a single published estimate, though; an adversary who differences overlapping slices or repeats queries averages the noise away, and must be limited separately.\n\nThese rules also close the inflation channel discussed in Malicious clients: an attacker who can read fine-grained per-slice counts gains a decoding oracle for their own forged tokens, and denying counts on sub-floor slices removes it. So the cardinality floor pays for itself three times over—anonymity in small populations, safety of published output, and resistance to inflation attacks.","title":"Reporting and publishing counts safely"},{"category":"section","location":"03-counting-users/#Anonymously-Counting-Users","page":"Anonymously Counting Users","text":"It’s hard to estimate the number of users of open source software. In the Julia ecosystem, whenever someone installs or updates packages, they connect (by default) to a global network of Pkg servers operated by the open source project. If each client were to send some metadata along with each request, allowing us to count them, then we could use the number of unique client installs making requests to Pkg servers to help figure out the number of Julia users. We also want package authors to be able to estimate the number of people using their specific software. This isn’t merely a matter of curiosity or bragging rights: research funding for open source software is often predicated on demonstrating impact—if you can say “our package was installed on 50,000 systems last year”, that’s a pretty strong argument for the impact of your project. As a community, we also want to understand other things about Julia users, including how many people use various operating systems, how many people are using different versions of Julia, and where in the world Julia users come from.\n\nThe simplest way to count unique installs would be for each Pkg client to generate a unique random value (e.g. a random 128-bit value) the first time they make a Pkg request, save it, and then send it as a header along with each Pkg server request. This was proposed at one point, but after much discussion it was deemed to be too much of a privacy hazard. Such a unique ID would allow anyone with access to Pkg server logs to track every Pkg request made by every client. Even with the ability to opt out, doing this by default is too invasive. If we’re going to count clients, we need to ensure anonymity. It should be impossible for anyone—even with full access to our Pkg server logs—to identify or track users making requests.\n\nThe naive random ID approach, even though it was deemed unacceptable for privacy reasons, will serve as a “gold standard” for us, at least in terms of functionality. We will judge our solutions by how close they get to providing equivalent functionality to unique client IDs. The “golden features” include:\n\nClients can generate their own IDs, servers do not need to issue them\nServers can passively collect request logs and post-process them\nClient counts can be computed within arbitrary slices of request logs.\n\nIdeally we want something that gives us similar architectural simplicity and flexibility but preserves user anonymity and privacy. Is this even possible? If we can come up with a user count estimation scheme that functionally approximates unique client IDs while preserving user anonymity, we will consider it a success.\n\nThe rest of this writeup is about exactly that: a new technique for estimating unique client installs in such a way that individual users are very hard to track or identify, even by people with full access to server logs. It makes two modest sacrifices compared to unique client IDs that have little impact on utility in practice:\n\nIt doesn’t allow client counting across completely arbitrary slices of requests—there are certain classes of requests across which client counts cannot be aggregated;\nIt is approximate rather than exact. We can estimate the number of users in each slice of logs with some random error. The error bounds, however, are small and tunable.\n\nOur approach combines two algorithms that each seem like magic: HyperLogLog and RSA. HyperLogLog is an algorithm for estimating the number of unique values in huge data sets using less memory than seems like should be possible. RSA (Rivest–Shamir–Adleman) is one of the original public key cryptography techniques, which, even though we now take it for granted, also doesn’t seem like it should be possible. The new protocol, which I’m tentatively calling “HyperLogLog Over RSA”, combines these magical technologies and does something new: it allows accurately estimating the number of unique clients making requests to a service without anyone being able to single out or track individual clients, even with full access to request logs.\n\nThroughout this writeup, I will use Julia’s client-server interaction as the grounding context, but user counting via HyperLogLog over RSA is a completely general technique that could be used by other open source projects, or anywhere one wants to estimate unique client counts while keeping individual clients anonymous in the aggregate. As above, the guarantee is strongest precisely where counting matters most—in large populations. Privacy laws like Europe’s GDPR and California’s CCPA make this not just a Good Thing™ to do, but also a legal requirement in many parts of the world.","title":"Anonymously Counting Users"},{"category":"section","location":"03-counting-users/#HyperLogLog","page":"Anonymously Counting Users","text":"HyperLogLog (HLL) is an algorithm for answering the question “How many unique values are there in this collection of values with repeats?” The naive way to do this, of course, would be to collect all the values and when you see a value, check if it’s in the collection of seen values. But that requires saving all the encountered values, which requires O(n) memory. HyperLogLog allows accurately estimating the number of unique values with O(log(log(n))) memory—whence the name. This grows so slowly that it is, for most practical purposes, constant. In most use cases a fixed upper bound on n is assumed, which makes the memory usage actually constant. Being able to count unique values with effectively constant memory, even approximately, is amazing.\n\nIt’s worth emphasizing that this is not one of those algorithms that has great theoretical properties but is impractical to use in reality. HyperLogLog is extremely practical and widely used. The constant factor in the asymptotic memory usage is quite small, and relative standard error is around 16 in a common configuration and can be made smaller with very modest increases in memory footprint.\n\nOur approach treats each client install as a “unique value” to count and uses HyperLogLog to estimate the cardinality of the set of those unique values. Recall that in the “gold standard” client counting algorithm, each client generates a random unique value. Instead of sending that unique random value, each client can derive a HyperLogLog value from it, and send that instead. Individual HLL samples are very small—only 18 bits in a common configuration—so they don’t encode nearly enough information to uniquely identify all clients. Moreover, since clients generate these values independently, the birthday paradox tells us that collisions occur long before there are 2^18 clients, and since HLL values are highly non-uniform, we actually start getting collisions around as few as 64 clients! This is a promising start but, as we’ll see, there are some issues.\n\nBefore we get to the problems with naively using HyperLogLog for counting clients, let’s develop an intuition for how and why HyperLogLog works. Suppose each client draws a random geometric sample value, i.e.\n\n0 has probability frac12\n1 has probability frac14\n2 has probability frac18\nk has probability frac12^k+1\n\nThe basic intuition of HyperLogLog is that if the maximum geometric sample observed is k then there are around 2^k unique values in the set. Of course, this estimator is terrible. It only ever estimates power-of-two cardinalities and has huge variance. HyperLogLog addresses both problems by uniformly splitting the space of clients into buckets, making a geometric estimate for each bucket, and then combining all those estimates into a single improved estimate. With enough buckets this actually produces a high quality estimate. The number of necessary buckets is not even inordinately large—in the common configuration we’ve mentioned, there are 2^12 = 4096 buckets, which gives relative standard error of 163, corresponding to a 95% confidence interval of 319. If more accuracy is desired, it’s relatively cheap to add more buckets and reduce the error bounds—the error is inversely proportional to the square root of the number of buckets.\n\nEach HyperLogLog sample has two parts:\n\nb: a uniformly distributed bucket index\nk: a geometrically distributed sample value\n\nThe common configuration we’ve referred to above has 2^12 = 4096 buckets and caps the geometric sample value at 63 so that the geometric value can be encoded with 6 bits and the estimates are capped at 2^63. This is plenty for many purposes, including ours. Each such HyperLogLog value can be encoded with only 18 bits: 12 bits for the bucket and 6 bits for the geometric sample. If we want to splurge and allow estimating cardinalities up to 2^127 we need only a single extra bit per HLL value. If we want to make our estimates twice as accurate, we have to quadruple the number of buckets, which only costs two extra bits per HLL sample. HyperLogLog’s extreme memory efficiency means every additional bit has great leverage. We will use the “typical” 18-bit configuration as a reasonable reference point throughout.\n\nA straightforward implementation of generating a random HLL pair is:\n\nb = rand(UInt16) >> 4\nk = min(63, trailing_zeros(rand(UInt64)))\n\nSuppose each client generates an 18-bit HLL sample value like this once, saves it, and then includes that value in the header of each Pkg request it makes. Then we can estimate the number of unique clients appearing in any subset of request logs by applying HyperLogLog aggregation and estimation to the HLL values for that set of logs. Since each client’s HLL value is only 18 bits, it’s impossible to uniquely identify most users—there just aren’t enough bits to distinguish more than 2^18 = 262144 different users—and since they generate values independently, collisions will start to happen long before that many users.\n\nThis seems pretty good. Can we just do this? I have, frankly, considered it. But there are two main issues that have given me pause:\n\nMost clients get small geometric sample values that are very common and thus anonymous, but some clients will get rare geometric values and be highly identifiable. HLL samples are anonymous on average, but an inherent aspect of how the estimator works is that some values are rare and thus not anonymous.\nHyperLogLog values are trivially forgeable. If a malicious client were to send requests with intentionally large geometric values, they could inflate the client count estimates arbitrarily high, rendering them useless. Worse: a malicious client only needs to send one forged request per bucket to push the estimate to the maximum.\n\nUsing HyperLogLog by itself for unique client estimation, while tantalizing, is not as anonymous as we’d like and requires a trivial amount of work to sabotage. Fortunately, both of these issues can be addressed.","title":"HyperLogLog"},{"category":"section","location":"03-counting-users/#Resource-class-sharding","page":"Anonymously Counting Users","text":"The first problem with HyperLogLog is that some clients get rare, highly identifiable hash values. This issue can be mitigated by sharding requests by resource class. The idea is to split requests into classes that are narrow enough that correlating a client’s behavior within each class is harmless yet broad enough that estimating within each class is still useful. Rather than having a single global HyperLogLog value, a client would have a different, independent HLL value for each resource class. There may be situations where such a split is not possible, but in Julia’s Pkg server context, it’s fairly clear how to do it—a resource class would be some prefix of a request URL’s resource path. For example, here’s how I would map resource paths to resource class prefixes:\n\n/registries longrightarrow /registries\n/registry/$uuid longrightarrow /registry/$uuid\n/package/$uuid/$hash longrightarrow /package/$uuid\n/artifact/$hash longrightarrow /artifact/$hash\n\nFor registries and artifacts, this is the full resource path; for packages, it lops off the package version hash, keeping only the package UUID. Note that this means that each client generates a new, statistically independent HyperLogLog value for each package and artifact that they request. This sharding scheme allows estimating:\n\nTotal Julia users via requests to /registries\nUsers of each registry via requests to /registry/$uuid\nUsers of each package via requests to /package/$uuid\nUsers of each artifact via requests to /artifact/$hash\n\nEstimates can be made for arbitrary slices of the request logs within each resource class too. We can, for example, aggregate at various time scales—daily, weekly, monthly, yearly. We can slice by region, operating system, Julia version, and any other data that the client shares with the servers. Or any combination of the above that may turn out to be of interest. There’s no need to anticipate which ways of slicing and dicing will be useful ahead of time—logs can be queried and aggregated arbitrarily after the fact.\n\nThis slicing freedom comes with one important caveat, which connects back to anonymity. A slice is a population restriction, and the anonymity of HyperLogLog counting depends on the population being counted being reasonably large. Slice finely enough—a single package, in one country, on one Julia version, in one hour—and the effective population can fall to a handful of clients or even one, at which point the bucket-and-geometric tag in that slice is unique and reporting it would single out individuals. The protocol therefore enforces a minimum-cardinality floor: it refuses to report (or publish) any slice whose estimated cardinality falls below a threshold, folding everything beneath the floor into a single “other” aggregate. This costs almost nothing in practice, because HyperLogLog is already a poor estimator at small cardinalities—the slices a floor suppresses are exactly the ones whose counts were least trustworthy anyway. We return to how to choose the floor, and to a subtlety about answering many overlapping queries, in the Protocol Summary.\n\nThe only thing that resource class sharding prevents is aggregating or correlating across classes. You cannot ask how many clients downloaded package A or B. You can only estimate A and B separately, you can’t estimate the union or intersection. This is a limitation, but it seems like an acceptable one. Whereas there is significant demand for knowing how many users there are of specific packages, there is very little call for estimating correlations between packages.\n\nHow does sharding help privacy? Even in the original, simple “each client sends a unique ID” scheme, resource class sharding makes the privacy situation considerably better. Imagine that instead of sending the same ID with each request, each client generates a “master key” and derives a “class-specific ID” for each request class by cryptographically hashing the master key with the resource class string. This prevents tracking users across resource classes since the IDs are different and the cryptographic hash prevents linking them. You could still see that the same client downloaded three versions of a package this month, but you’d have no way to connect that to the same client downloading any other packages. This is actually not a bad scheme, but I suspect that some people would still strongly object to clients being uniquely identifiable at all, and it may still run afoul of privacy laws like GDPR.\n\nResource class sharding with HyperLogLog works in much the same way except that instead of sending the derived class-specific ID, the client uses the hash of the master key with the class string to generate a HyperLogLog value and sends that along with the request. The HLL values in different classes are statistically independent thanks to the cryptographic hashing. In a given resource class, only a few unlucky clients will have rare, uniquely identifiable values. Crucially, clients that are rare in one class will be common in most other classes. Even if a client is unlucky enough to be rare in two different classes, the server has no way of knowing that those rare HLL values belong to the same client.\n\nSharding also improves fairness. Without sharding, most clients are unremarkable, but a few, unlucky clients have uncommon, identifiable HLL values. This is unfair: some clients are trackable, most aren’t. With sharding, every client will be unremarkable in most classes and rare in a few. Since there are an unbounded and, in practice, large number of resource classes—one per package/artifact—each client will be anonymous in most of them and rare in only some. This is a much fairer and more symmetrical situation.\n\nOne may object that some resource classes are surely more important than others. For example, suppose a client has a rare HLL value in the /registries class. Doesn’t this seem more significant than having a rare sample in some obscure package’s class that the client may never even download? Perhaps, but consider that the only thing you can learn about a client with a rare value in the /registries class is how often they check for new registries—information that is not interesting or sensitive in the slightest. Not accidentally either: this information is uninteresting precisely because everyone requests /registries regularly. If a package has very few users, on the other hand, then it’s more notable if some user does download it, but for the very same reason, none of the package’s users is likely to have an especially rare HLL value. The rarity of the most uncommon HLL value in a class is inherently inversely related to the popularity of that resource class: the rarest HLL sample values will tend to happen in classes that are the most popular—and popular classes are the ones that are the least interesting to learn anything about. With sharding, the most identifiable HLL values tend to occur in the least interesting classes and the most interesting classes tend to have the least identifiable HLL values.\n\nSharding is an effective privacy measure because almost everything of interest to an attacker comes from following users across packages, which is precisely what sharding blocks. On the other hand, correlating users across packages is of fairly minor interest for legitimate purposes, and losing that ability is a small sacrifice for a significant privacy gain.","title":"Resource class sharding"},{"category":"section","location":"03-counting-users/#Signed-HLLs?","page":"Anonymously Counting Users","text":"The second major issue with clients generating and sending raw HyperLogLog values is how easy it is for a malicious client to forge large geometric values and sabotage client count estimates. If a single malicious client sends k = 63 in every bucket, then the client count estimate is inflated to 19 quintillion (billion billion) clients, which is obviously absurd, but also makes all our estimates useless. In other words, the entire system can be brought to its knees with a tiny number of malicious requests—only one per bucket, of which there are only a few thousand. If the attack is that blatant, of course, it’s easy to filter out the requests with the implausible k = 63 geometric sample values, but a more subtle attack could render estimates incorrect without being so easy to detect or filter.\n\nTo prevent spoofing, it seems like a client would need to present, alongside its HyperLogLog value, some evidence that it got that value legitimately and didn’t just “make it up.” Some kind of stamp of authenticity—a digital signature. A simple signature system would be for the server to generate a random secret key and cryptographically hash the HLL value along with that key, and use the hash value as a signature. If the client presents the correct hash value with its HLL value, then the pair is valid; otherwise it should be ignored for client counting purposes. Unfortunately, there are several problems with signed HLL values—some are solvable while others are not.\n\nThe first problem with signed HLL values is that many signature schemes could themselves act as a unique client fingerprint, which is exactly what we’re trying to avoid. It doesn’t matter if the HLL value is only a few bits if the signature that accompanies it is large enough to uniquely identify billions of clients. Maybe the signature is a pure function of the HLL value, but how can the client know that? A protocol that’s supposed to protect client anonymity needs to not only be resilient to misbehaving clients, but also prevent malicious servers from surreptitiously tracking users. Unless the client has a way to verify that the signature attached to an HLL value is the unique, correct signature for that value, then a signature scheme can be used by a malicious server to track clients.\n\nThis problem is solvable: public key cryptography allows signatures that can be checked but not forged. The server publishes a public key and signs each HLL value with its corresponding private key. A client can check that the signature is correct using the public key. Depending on the public key system chosen, additional care may need to be taken to ensure that multiple different signatures cannot appear correct, but this is a minor technical detail. Public key cryptography applied carefully can fully solve this issue.\n\nA second problem with HLL signatures is that we still want to do resource class sharding. This means that a client needs a different signature for each resource class and it has to serve the same HLL value for that resource class every time (otherwise we’re counting requests, not clients). The obvious way to do this is for the client to request a new HLL value the first time it makes a request in a given class and then store that value to be used in the future. Assuming 256 bits per resource class key, 18 bits per HLL value, 512 bits per signature (common for state-of-the-art elliptic curve signatures; other systems have larger signatures), that’s about 100 bytes per resource class. If the typical user installs 5k packages over time, that’s half a megabyte of storage. Given modern hard disk capacity this is certainly feasible, but that’s a lot of data for each client to keep around for the sake of something that arguably doesn’t benefit the individual user directly. Even if it is feasible, it has an “optics” problem: people may see this data and think “Why are they storing all this 'telemetry' data about me?” Yes, we can explain how the data isn’t used to track you, but it doesn’t look great.\n\nA third problem with signed HLL values is that it adds operational complexity to our servers. This can certainly be done, but it comes at a cost. We have carefully designed the Pkg server system to be simple and reliable to run. The /registries endpoint is the only Pkg endpoint that ever changes—everything else that a Pkg server serves is immutable and content addressed and thus perfectly cacheable without possibility of invalidation. Even the /registries endpoint can be implemented as a small, static file that just needs to be updated when registry content changes. This means everything in the Pkg protocol is straightforwardly cacheable: most paths can be cached forever, and /registries can be cached with any reasonably short time to live. This makes putting a content-delivery network (CDN) in front of Pkg servers trivial, and we lean quite heavily on CDNs. An endpoint handing out randomly generated, signed HLL values would require complex, dynamic logic on a server, would be inherently uncacheable, and could not be served through a CDN. All feasible, but not ideal.\n\nThe last and most critical problem with signed values is that they still allow malicious clients to observe and selectively choose HyperLogLog values with large geometric values. If the HLL values are sent in the clear, it’s trivial to request as many as one wants and selectively choose the rarest one. This can be partially addressed by encryption: instead of sending HLL values in the clear, encrypt them. We can’t use off-the-shelf encryption algorithms since they all have block sizes of at least 64 bits—if we encrypt an 18-bit HLL value with a 64-bit block cipher, we get an opaque 64-bit value with plenty of bits to fingerprint clients. Fortunately, one can easily roll a bespoke Feistel cipher whose block size matches our HLL size. Since the encrypted payload is the same size as an HLL value, and the signature can be checked to be a pure function of the encrypted HLL value, the client can be sure that only one HLL value’s worth of identifying information is being submitted with each request.\n\nEncryption doesn’t entirely solve the observability problem, however: even if they’re encrypted, a malicious client can request a large number of HLL values and observe which values are served most often—they can statistically reconstruct the distribution of HLL values by observation. They can submit the rarest ones they get as often as they want and bias our client count estimates. This is not as severe a problem as an attacker being able to forge arbitrarily large geometric samples, but it means that they can inflate user count estimates.\n\nIs this significantly worse than the “gold standard” of random unique client IDs? Unfortunately, it is. With client IDs, if an attacker wants to inflate the client count for a slice of requests by n extra clients, they have to make n requests. With encrypted, signed HLL values, the attacker has to do a lot of work up front to find rare values, but once they have some rare values, they only need to send one request per bucket in a slice in order to inflate its client count. Of course, they don’t know which values are in which buckets, but they can submit all of the rarest values that they’ve found (presumably whichever ones they’ve only seen a single time). This means that given enough up front work finding rare HLL values, any slice of requests can be inflated arbitrarily with a fixed amount of attack-time effort.\n\nAny one of these problems might not be fatal on its own, but between the size of data storage required, the additional operational complexity, and the fact that an attacker can still inflate estimates with a fixed amount of effort, signed HyperLogLog values are, unfortunately, not an appealing solution for client count estimation. If this was the best we could do, I might consider deploying it, but I would have significant reservations. Fortunately, however, signatures are not the only possible solution.","title":"Signed HLLs?"},{"category":"section","location":"02-for-cryptographers/#For-Cryptographers","page":"For Cryptographers","text":"This is a compressed, self-contained tour of the construction for readers comfortable with finite abelian groups and RSA. Everything here is developed in full in the numbered sections; the goal of this page is to let you see the whole shape quickly and decide where to dig in. Readers preferring a more gradual development from first principles should start with the next section.\n\nThe protocol decouples into two parts that can be understood independently:\n\na HyperLogLog (HLL) sketch, which carries the entire privacy model, and\nan RSA encoding of that sketch, which carries the entire integrity model and is provably privacy-neutral.\n\nThe design target is a per-request token y in Z_N^* such that\n\nit reveals exactly the client’s HLL sketch value (b k) and nothing else;\nit can be freshly re-randomized on every request without knowing the factorization of N, so repeated tokens from one client are unlinkable beyond sharing a sketch value; and\nits sketch value cannot be read or biased by the client without that factorization, so clients cannot manufacture rare values to inflate counts.\n\nProperties (1) and (2) are what make it private; (3) is what makes the counts trustworthy.","title":"For Cryptographers"},{"category":"section","location":"02-for-cryptographers/#What-leaks:-the-HLL-sketch","page":"For Cryptographers","text":"Fix public parameters: an odd bucket count B and a maximum rank m ge 2. Each client holds a persistent per-class sketch\n\nhll = (b k) in Z_B times set0 1 dots m\n\nwith b uniform and k geometric, Prk = j = 2^-(j+1) for j  m. The server takes the per-bucket maximum of k over any subset of requests and applies a standard HLL estimator to recover the number of distinct clients in that subset. Everything the server is supposed to learn about a client is contained in (b k); the privacy argument — on-average anonymity within an equal-sketch class, and statistical independence across classes via sharding — is made entirely at this level in Anonymously Counting Users. The remainder of this page is about encoding (b k) into an RSA group so that a client can neither read nor forge it, without changing what it leaks.","title":"What leaks: the HLL sketch"},{"category":"section","location":"02-for-cryptographers/#The-ring-and-its-coordinates","page":"For Cryptographers","text":"Take\n\nN = PQ = (2Bp + 1)(2^m q + 1)\n\nwhere P Q p q are distinct odd primes coprime to B, which is odd. Then P equiv 3 and Q equiv 1 pmod 4, so N equiv 3 pmod 4, and because P - 1 = 2Bp and Q - 1 = 2^m q split into pairwise-coprime cyclic factors (B odd is what keeps C_B off the 2-part),\n\nZ_N^* cong C_2 times C_B times C_2^m times C_pq\n\nFix a semigenerator g: an element whose projection into each cyclic factor generates that factor. Every x in Z_N^* then has well-defined coordinates\n\nlog_g x = (a b c d) in Z_2 times Z_B times Z_2^m times Z_pq\n\nmultiplication adds coordinates, and exponentiation by t scales them by t. The sketch is read off the middle two:\n\nhll(x) = big(b tz(c)big)\n\nwhere tz(c) = v_2(c) is the 2-adic valuation (trailing-zero count, with tz(0) = m). For uniform x, b is uniform on Z_B and tz(c) is geometric on set0 dots m — exactly the HLL distribution (made rigorous in Geometric RSA rings). The remaining coordinates are noise to be washed out: a in Z_2 only tracks the Jacobi symbol, and d in Z_pq is pure per-client identity entropy.","title":"The ring and its coordinates"},{"category":"section","location":"02-for-cryptographers/#The-token,-and-why-it-leaks-only-the-sketch","page":"For Cryptographers","text":"A client draws a persistent secret x_0 in J_N^- (Jacobi symbol -1) and derives its per-class secret by hashing into the exponent, x = x_0 g^H(x_0 textclass). Per request it samples\n\nw in W = (Z_N^*)^B2^m qquad\nt in T = set 2Bi + 1 st i in 0 2^m-1) \n\n— both samplable without the factorization — and sends y = wx^t bmod N. The server discards any y with Jacobi_N(y) ne -1.\n\nThe whole construction is in what (w t) do coordinate-by-coordinate:\n\ncoord. factor multiplier w in W exponent (cdot)^t t = 2Bi+1 net effect / exposed\na C_2 fixed fixed (t odd) pinned by J_N^- — not per-client\nb C_B fixed fixed (t equiv 1 bmod B) exposed: the bucket\nc C_2^m fixed times uniform odd unit randomized within its valuation class — only tz(c) survives: the rank\nd C_pq to uniform (irrelevant) washed out\n\nTwo facts make the table work:\n\nW is the image of the B2^m-power map. The orders of C_2 C_B C_2^m all divide B2^m, while gcd(pq B2^m) = 1, so that map annihilates the first three factors and is an automorphism on the last: W = set0 times set0 times set0 times C_pq. Multiplying by uniform w therefore replaces d with fresh uniform randomness and touches nothing else.\nFor t = 2Bi + 1: t equiv 1 pmod B and t is odd, so b and a are fixed; and as i ranges over 0 2^m-1), t bmod 2^m runs over every odd residue exactly once, so t acts as a uniform unit on c — preserving v_2(c) = tz(c) but uniformizing c within that valuation class.\n\nHence the (w t)-orbit of x is exactly the fiber hll^-1(hll(x)) cap J_N^-, and y is uniform on that fiber, not merely supported on it. Two honest clients with the same sketch induce identical token distributions, and the server’s entire view is a function of (b tz(c)). This is the anonymity theorem of Proof of Anonymity; the only bookkeeping subtlety is the 2-torsion (C_2/Jacobi) accounting, which is exactly why tokens are confined to J_N^-. The proof is phrased in these semigenerator coordinates throughout — if there is a more standard way to present it, I’d welcome the pointer.","title":"The token, and why it leaks only the sketch"},{"category":"section","location":"02-for-cryptographers/#Integrity-I-—-malicious-clients-(unforgeable-rank)","page":"For Cryptographers","text":"A client who could steer hll(x) toward large k could inflate counts cheaply, so we need clients unable to do better than chance. Without the factorization a client knows neither b nor c for any value it can produce. It can walk an arithmetic progression of exponents, shifting c by a chosen h, but it cannot see tz(c + h), so reaching rank ge k costs sim 2^k tries — exactly chance. Inflation is therefore linear at approx 1ln 2 approx 144 forged requests per unit of count (the unique-ID gold standard is 1); see Malicious clients, which also notes that this linear bound assumes the attacker has no per-slice count oracle, and that a published-count interface must therefore floor small slices.\n\nThis is the one acknowledged gap: there is no reduction from biasing the rank to factoring or another standard RSA assumption. The informal evidence is that recovering the c-coordinate of a J_N^- element with respect to g appears as hard as the discrete-log–flavored problems RSA already leans on — the Jacobi_N = -1 requirement is what blocks the obvious bootstrap from known powers of g (which all have Jacobi_N = +1). But this is a conjecture. It is also the less critical half of the formalization: a broken integrity bound costs accuracy, while a broken privacy bound costs anonymity; only the latter is proven. A reduction proving that biasing the rank is hard would be the most valuable single addition to this work.","title":"Integrity I — malicious clients (unforgeable rank)"},{"category":"section","location":"02-for-cryptographers/#Integrity-II-—-malicious-servers-(fingerprint-freedom)","page":"For Cryptographers","text":"The server chooses N, and a maliciously structured N can turn the token into a fingerprint. For instance, if, instead of the above structure, we have\n\nN = (2^m B p + 1)(2^m B q + 1)\n\nthis gives Z_N^* two C_B and two C_2^m factors; the same honest token then exposes 2log_2 B + m approx 87 bits — total deanonymization. So a client must verify, without learning the factorization, that N has benign structure. The exact criterion (Malicious servers) is\n\nbegingathered\nN equiv 3 pmod 4 05em\ngcd(B N) = gcd(B N-1) = 1 05em\nJ_N^+  W_N text cyclic of order dividing  B2^m\nendgathered\n\nThe third condition is what caps the leak at one sketch’s worth of bits, and — combined with the first two — it holds iff N has at most two distinct prime factors.\n\nThat equivalence rests on a clean quadratic-residue criterion. For odd N with D distinct prime factors, every pair setx y subseteq J_N^+ has at least one of setx y xy a quadratic residue iff D le 2; and for D ge 3, a uniform pair has this property with probability at most 58. So the server proves D le 2 by answering challenges: for random pairs setx y subseteq J_N^+ it returns a square root of one of x, y, xy. A D ge 3 modulus survives n independent challenges with probability le (58)^n; to force a forging server to try ge alpha candidate moduli, take\n\nn = ceilfraclog_2 alphalog_2(85)\n\nDrawing the challenge pairs by hashing N makes this a non-interactive certificate (B m N g textsquare roots). Verification is one Jacobi symbol, two gcds, and n squarings.\n\nTwo cautions on alpha (detailed in Malicious servers): a forger knows its own candidate’s factorization, so the QR checks are free for it and it fails fast, meaning alpha must be sized against attacker wall-clock rather than a bare trial count. Since n grows only as log alpha, the certificate stays tiny regardless — so use alpha ge 2^lambda for the system security level lambda, never below 2^80, with alpha = 2^128 (n = 189, ~24 KB) the comfortable default.","title":"Integrity II — malicious servers (fingerprint-freedom)"},{"category":"section","location":"02-for-cryptographers/#What-is-proven,-and-what-is-not","page":"For Cryptographers","text":"Property Status Where\nhll induces the correct HLL distribution elementary; empirically confirmed Geometric RSA rings\nAnonymity: equal-sketch tokens are identically distributed (uniform on the fiber) proven Proof of Anonymity\nFingerprint-freedom + a certificate with explicit (58)^n soundness proven Malicious servers\nRank-unforgeability by malicious clients argued, not reduced to a standard assumption Malicious clients\n\nFeedback is welcome everywhere but especially on these three points:\n\nA more standard presentation of the semigenerator-coordinate argument in Proof of Anonymity;\nAny route to a hardness reduction for client-side rank-unforgeability;\nWhether the geometric-distribution argument in Geometric RSA rings wants more rigor than it currently has.","title":"What is proven, and what is not"},{"category":"section","location":"01-executive-summary/#Executive-Summary","page":"Executive Summary","text":"I’m proposing a new (as in novel) way to estimate how many unique clients use Julia—and each package, sliced by OS, version, and region—without being able to track anyone. It stacks three layers:\n\nResource-class sharding\nHyperLogLog sampling\nRSA encryption\n\nThe first two layers provide the privacy: you can’t follow a client across packages, and every client hides in a crowd of lookalikes in most classes. The RSA layer doesn’t change the privacy model, but stops clients from faking their samples to skew the counts. So if you accept the privacy of sharding + HLL and believe two short proofs about the RSA layer, then you should be ok with the whole protocol.\n\nI’d like to implement the client side of this protocol in Julia 1.14, and then use the data the server collects to publish client-count estimates alongside other Pkg server stats.","title":"Executive Summary"},{"category":"section","location":"01-executive-summary/#Overview","page":"Executive Summary","text":"We’d like to estimate how many people use Julia—and how many use each individual package—without tracking anyone. This isn’t idle curiosity: open source funding and project survival often hinge on demonstrating impact (“installed on 50,000 systems last year”), and package authors deserve to know whether anyone is actually using their work. We’d also like aggregate breakdowns by operating system, Julia version, and region. The obstacle is that the obvious way to count unique installs—have each client send a persistent random ID—was rightly rejected as a privacy hazard: it would let anyone with access to Pkg server logs follow every request a client ever makes.\n\nSomewhat surprisingly, there isn’t any really good technique for this in the literature—at least not that I or my research assistants (Claude and ChatGPT) could find. To address this gap—and frankly to scratch an annoying itch—I’ve developed a new protocol for counting clients while keeping individuals anonymous. I’m tentatively calling it “HyperLogLog Over RSA” and I’m posting here to give people a chance to look it over, comment, and raise any objections they might have to using it in Pkg.\n\nThe protocol’s privacy model is built in three layers, and a nice aspect of the design is that you can evaluate them independently. In particular, the privacy of the full system is exactly the privacy of the first two layers. The third layer—the RSA cryptography—changes nothing about what the server learns about a client; it only keeps the clients from messing with the counts. So if you’re comfortable with layers 1 and 2, and you trust the proofs about layer 3, then you’re comfortable with the whole protocol. This means that you don’t need to understand the complicated math to understand the privacy model. Well, there’s a little math to the HyperLogLog part, but far less than there is in the RSA part.","title":"Overview"},{"category":"section","location":"01-executive-summary/#Layer-1-—-Resource-class-sharding","page":"Executive Summary","text":"Instead of one identity per client, each client uses a separate, unlinkable identity for each “resource class”: the top-level registry, each registry, each package, each artifact. Concretely, a client keeps a single master key and derives its per-class value by cryptographically hashing that key with the class name—so it never has to store anything per class. Because the classes are cryptographically independent, the server can count unique clients within a class but can never link a client’s activity across classes. You cannot follow a user from one package to another.\n\nThis already removes the scariest capability of the unique client ID scheme: cross-package tracking. Resource class sharding would help privacy tremendously even if we kept full, unique per-class IDs. Sharding buys significant privacy on its own. What it doesn’t fix is that, within a single class, each client would still be uniquely identifiable if we used unique IDs. But we don’t have to do that to get decent estimates.","title":"Layer 1 — Resource class sharding"},{"category":"section","location":"01-executive-summary/#Layer-2-—-HyperLogLog","page":"Executive Summary","text":"HyperLogLog is a pretty famous cardinality estimation algorithm. In our context we can use it to provide on-average anonymity within resource classes. We replace a large, unique ID with a tiny, deliberately lossy sample: a uniformly random 12-bit bucket index plus a small (6-bit), geometrically distributed value. That’s about 18 bits of information about each client. In a class with many more clients than there are buckets (B approx 4096), that is far too little to pick anyone out: the average client shares its sample with a healthy crowd of lookalikes and is genuinely anonymous. HyperLogLog’s near-magical property is that one can still estimate the number of unique clients from these samples, to a tunable accuracy, even though it does not seem like there are nearly enough bits for that.\n\nThe honest catch is in the tail. The geometric value is heavily skewed: most clients draw one of the small, common values and vanish into the crowd, but a few unlucky clients draw a large, rare value that almost nobody else shares—and that makes them essentially unique within the class. So within a single class, HyperLogLog is anonymous on average: the typical client is well hidden, but the most identifiable clients are not.\n\nThis is exactly the unfairness that sharding (Layer 1) cures. Because a client’s sample is independent in every class, the clients who stand out here are not the ones who stand out there: every client draws a rare, identifiable value in a few of its many classes and a common, anonymous one in the overwhelming majority. So instead of a fixed unlucky minority being trackable everywhere, each client is conspicuous in a handful of its classes and invisible in the rest—and even where it is conspicuous, that only links its own requests within that one class (i.e. one package’s update cadence), never across classes. Sharding turns “anonymous for the average client” into “anonymous on average (across classes) for everyone.”\n\nTwo honest limits remain. A class—or a fine-grained slice of one—with only a handful of clients in it can’t hide anyone, no matter the scheme: there’s simply almost no one to blend in with. You can probably enumerate every FreeBSD user from the OS field alone (sorry, Alex). That’s inherent to counting a small population rather than anything specific to HyperLogLog, and we handle it by declining to report counts for slices below a minimum size. And all of the above concerns the value a client sends, not network-level identifiers like its IP address—those carry their own tracking risk and must be handled separately, as they are today.\n\nIf you’re still reading and you can accept the privacy model of HyperLogLog counting with resource class sharding, you’ve accepted the entire privacy model. Everything below is about integrity—ensuring that badly behaved clients cannot mess with the estimates too much—not privacy.","title":"Layer 2 — HyperLogLog"},{"category":"section","location":"01-executive-summary/#Layer-3-—-RSA,-for-integrity-only","page":"Executive Summary","text":"The two layers above completely define what the server can learn. The RSA layer’s sole job is to stop misbehaving clients from skewing the counts. With plaintext HyperLogLog, one client could spoof a handful of requests carrying large geometric values and inflate an estimate arbitrarily. RSA fixes this by letting clients sample a HyperLogLog value without being able to see or choose it—so the only way to push a count up is to send more requests, at a near-optimal cost of about 1.44 requests per unit of inflation (the naive unique-ID scheme’s cost is exactly 1).\n\nTwo proofs make the privacy promise precise:\n\nAnonymity is preserved exactly. The encryption and the per-request re-randomization reveal no more about a client than a plaintext HyperLogLog value would. Layer 3 cannot make privacy any worse than layers 1 and 2 already are. (Proven in Proof of Anonymity.)\nThe server can’t smuggle in a fingerprint. A maliciously constructed RSA modulus could secretly tag clients, so the server must publish a certificate proving its modulus is “fingerprint-free.” Clients verify this certificate before using a ring. (Proven in Malicious servers.)\n\nIf those proofs hold—and reviewing them is exactly the scrutiny this writeup invites—then RSA is purely a guardrail on honest counting, invisible to the privacy analysis.","title":"Layer 3 — RSA, for integrity only"},{"category":"section","location":"01-executive-summary/#Two-modest-costs","page":"Executive Summary","text":"Compared to the rightly rejected unique-ID scheme, this approach gives up two things, neither of which matters much in practice:\n\nCounts can’t be aggregated across resource classes. You can estimate the users of package A and of package B separately, but not of “A or B”. There’s little demand for cross-package correlations anyway, so this is no great loss to us.\nEstimates are approximate rather than exact, with small and tunable error (more buckets ⇒ tighter error, at a few extra bits per sample). This is also ok since we don’t need to know the exact number of clients—being within a few percentage points is fine.","title":"Two modest costs"},{"category":"section","location":"01-executive-summary/#Why-this-is-worth-doing","page":"Executive Summary","text":"For Julia specifically, this would let us estimate total active installs, per-package user counts (real impact numbers for package authors and the funders who support them), and breakdowns by OS, version, and region. Because the design is built to be privacy-preserving, we can make it opt-out rather than opt-in, while still providing a way to turn it off for those who don’t want it. Opt-out is fairly necessary for this kind of thing, since most people leave the default alone (though we don’t know exactly how many). A persistent identifier like a client UUID counts as personal data under regulations like the GDPR and CCPA, so a counting scheme we can turn on by default has to be one that genuinely protects users; that is precisely what this design aims to be.\n\nIf you want to dig into the details: HyperLogLog and resource class sharding are covered in Anonymously Counting Users, and the two proofs are in Proof of Anonymity (anonymity) and Malicious servers (fingerprint-freedom).","title":"Why this is worth doing"},{"category":"section","location":"04-hyperloglog-over-rsa/#HyperLogLog-Over-RSA","page":"HyperLogLog Over RSA","text":"What alternative is there to signing HyperLogLog values? In the last section we encrypted HLL values. What if clients could sample encrypted HLL values for themselves? Of course, the way we discussed encrypting them previously, each distinct HLL value would have an equal chance of being chosen, which is entirely the wrong distribution—the whole point is that it’s geometric. But can we encrypt HLL values so that sampling encrypted values has the right distribution? This requires more common HLL values to appear in distinct encrypted forms many times: the most common values would have to appear 2^m-1 times as often as the least common ones, where m is the maximum geometric sample value. This is certainly possible—we can just encrypt a 128-bit unsigned random value and derive the HLL value from it on the server side. But that’s a massive client fingerprint. We need another crucial ingredient: the client needs to be able to randomize what they send so that any client with the same HLL value could have sent that value. This seemingly impossible trick turns out to be possible in the mathematical setting of RSA rings.","title":"HyperLogLog Over RSA"},{"category":"section","location":"04-hyperloglog-over-rsa/#RSA-ring-refresher","page":"HyperLogLog Over RSA","text":"An RSA ring is a modular integer ring, Z_N, where N is a large, balanced semiprime—i.e. a product of two similarly sized primes: N = P Q. When N is large enough, this is a hard factoring problem—hard enough that it is currently impossible when log_2(N)  1024. For good measure, people often use 2048-bit and 4096-bit semiprimes these days. Constructing such an N, on the other hand, is straightforward and takes no more than a fraction of a second: find two distinct primes, P and Q, of appropriate size, and multiply them to get N = PQ. The party that generates N can safely publish it and keep P and Q secret. This gives them knowledge of the structure of Z_N that no one else has, yet everyone can perform computations in Z_N. This was one of the first public-key encryption schemes and is still widely used. If all you want to do is public-key cryptography, the additional structure of Z_N is actually a liability and has to be carefully tiptoed around. But for us the additional structure is crucial to making what we want to do possible.\n\nThe classic use of an RSA ring is for public-key cryptography. The party that generates N chooses an exponent, E, and computes D = E^-1 bmodlambda(N). Here lambda(N) is the Carmichael function, defined as the smallest exponent such that for all x in Z_N, x^lambda(N) = 1 bmod N. This function is crucial for understanding exponents in Z_N^* because exponents behave modularly as well, but with a modulus of lambda(N): i.e. x^a = x^b bmod N whenever a = b bmodlambda(N). For semiprime N = PQ, we have lambda(N) = lcm(P-1 Q-1), which is easy for someone who knows P and Q to compute, but infeasible for someone who only knows N. Since DE = 1 bmodlambda(N) we have x^DE = x^1 = x bmod N for all x in Z_N^*. Thus, the exponents E and D form a public/private key pair:\n\nYou can publish E and anyone can encrypt a message x in Z_N^* by exponentiating by E: c = x^E bmod N.\nOnly someone who knows D can decrypt the message by exponentiating by D: c^D = (x^E)^D = x^DE = x bmod N.\n\nThis system can also be used for signatures. To sign a message, x, the party who knows D computes s = x^D bmod N and sends (x s) as a message/signature pair. Anyone with the public key can check the signature like this: \n\nbeginaligned\ns^E = (x^D)^E = x^DE = x bmod N\nendaligned\n\nIf s^E doesn’t match the message, the signature is invalid. Anyone can check a signature, but only someone who knows E can generate a valid one, so a valid signed message proves that the message is authentically from the party who knows E.\n\nWe will not actually be using RSA rings for public-key cryptography or signatures. Instead, we’ll encode HyperLogLog values in Z_N^*. Only the server who constructed N and knows the factors P and Q can decode the HLL values. The structure of Z_N^* also allows the client to randomize what they send so that the HLL value and only the HLL value is preserved by this randomization. This makes two clients with the same HLL value indistinguishable from the server’s perspective—the server learns the client’s HLL value and nothing else. Finally, this can all be done in such a way that the client only needs to generate and store a single “master key” value and can quickly and reproducibly generate resource-class-specific HLL values that are statistically independent from each other.","title":"RSA ring refresher"},{"category":"section","location":"04-hyperloglog-over-rsa/#Geometric-RSA-rings","page":"HyperLogLog Over RSA","text":"The first thing we’ll tackle is encoding geometric samples in an RSA ring with the right frequency distribution. To do this, we define a special class of RSA rings with some additional structure. Consider the following special shape of “geometric RSA ring”:\n\nbeginaligned\nN = P Q = (2 p + 1)(2^m q + 1)\nendaligned\n\nHere P, Q, p and q are all primes and m is some positive integer. This ring’s structure is Z_N cong Z_P times Z_Q, which cannot be broken down further as rings since P and Q are prime. But if we drop the additive structure and consider only the multiplicative structure, then we can break it down further into a product of cyclic groups:\n\nbeginaligned\nZ_N^* cong (C_2 times C_p) times (C_2^m times C_q)\nendaligned\n\nHere C_n denotes a cyclic group of order n, meaning that there is some generator element, g in C_n, such that g^n-1  1 and g^n = 1 and every element of C_n is of the form g^k for some k. When n is prime, every element besides g^0 = 1 is also a generator; when n is composite, it’s more complicated. This structure connects back to the Carmichael function and the modular exponential structure of Z_N^*. We choose a “semigenerator” element, g in Z_N^*, such that fmod(gP) and fmod(gQ) are generators in Z_P^* and Z_Q^*, respectively. Each component of g is a generator of that cyclic component, so if we take the elementwise logarithm of the cyclic components of g with respect to itself then by definition we have:\n\nbeginaligned\nlog_g(g) = (1 1 1 1)\nin Z_2 times Z_p times Z_2^m times Z_q\nendaligned\n\nThis is analogous to how log_x(x) = 1 for any positive real x. An arbitrary unit, x in Z_N^*, can be expressed in terms of its logarithm vector with respect to g:\n\nbeginaligned\nlog_g(x) = (a b c d)\nin Z_2 times Z_p times Z_2^m times Z_q\nendaligned\n\nThe behavior of Z_N^* can be entirely understood in terms of elementwise modular operations on these vectors of logarithms. Suppose we have x_1 and x_2 such that\n\nbeginaligned\nlog_g(x_1) = (a_1 b_1 c_1 d_1) \nlog_g(x_2) = (a_2 b_2 c_2 d_2) \nendaligned\n\nThen we have:\n\nbeginaligned\nlog_g(x_1 x_2)\n= (a_1 b_1 c_1 d_1) + (a_2 b_2 c_2 d_2) \n= (a_1 + a_2 b_1 + b_2 c_1 + c_2 d_1 + d_2) 05em\nlog_g(x_1^t)\n= t (a_1 b_1 c_1 d_1) \n= (t a_1 t b_1 t c_1 t d_1) \nendaligned\n\nProducts in Z_N^* turn into vector addition of exponents and exponentiation turns into vector scaling of exponents.\n\nIt’s worth emphasizing that the exponent in each component is modular with respect to its own modulus. This is especially significant when exponentiating by t, which manifests as scaling all exponents by t, since scaling by the same value can have very different effects on components based on their respective moduli. The Chinese Remainder Theorem guarantees that a single value t can be chosen to have independent effects on coprime components, which is very powerful. Components whose moduli are not coprime, on the other hand, will always be scaled in lock step up to the greatest common divisor of their moduli. For example, if we want to scale b in Z_p by t_p and d in Z_q by t_q then we can find t such that t = t_p bmod p and t = t_q bmod q and have precisely the desired effect. If we want to scale a in Z_2 by t_2 and c in Z_2^m by t_2^m, on the other hand, we are forced to have t = t_2 = t_2^m bmod 2, so they cannot be chosen independently—t_2 and t_2^m must have the same parity.\n\nThe key fact about our special RSA ring is that the multiplicative orders of elements of the C_2^m component have precisely the geometric distribution we need for HyperLogLog. Here is a table of element orders, how many elements have that order, the probability of randomly choosing such an element, and the corresponding number of trailing zeros for a geometric sample value:\n\nOrder Count Probability Trailing zeros\n2^m 2^m-1 dfrac12 0\n2^m-1 2^m-2 dfrac14 1\n2^m-2 2^m-3 dfrac18 2\nvdots vdots vdots vdots\n4 2 dfrac12^m-1 m-2\n2 1 dfrac12^m m-1\n1 1 dfrac12^m m\n\nThis correspondence is more than just lucky coincidence. If g is a generator for C_2^m, then every element of C_2^m is of the form g^k where  k in Z_2^m. The order of  g^k  in this group is determined by the largest 2^j that divides  k—i.e. the number of trailing zeros when  k is written in binary:\n\nIf  k is odd then g^k is another generator with full order, 2^m\nIf k has a single trailing zero then  g^k has order 2^m-1\nIf  k = 2^m-1 then g^k = -1, the unique order 2 value\nIf  k = 2^m = 0 pmod2^m then g^k = 1, the unique order 1 value\n\nIn other words, sampling an element from C_2^m and taking its order is fundamentally the same operation as sampling an m-bit integer and counting its trailing zeros—specifically the trailing zeros of its logarithm with respect to a generator (any generator—it doesn’t matter which one).\n\nSuppose we take a random element of our ring, x in Z_N. If x isn’t in Z_N^* then we can’t take its logarithm. It’s straightforward to test if x is invertible, however: just check that gcd(xN) = 1. For large, semiprime N, there is an astronomically small chance that x isn’t invertible. There are PQ elements in Z_N and (P-1)(Q-1) = PQ - P - Q + 1 elements in Z_N^*, so the probability of getting a non-invertible element at random is:\n\nbeginaligned\nfracP + Q - 1PQ\n= frac1P + frac1Q - frac1PQ\nendaligned\n\nIf we assume that P approx Q are large, which should be the case for RSA rings, then this is about 2P. When P and Q are large enough to be cryptographically secure, this is vanishingly small. Moreover, if someone happens to sample a non-zero x that isn’t coprime with N, they have lucked into being able to factor N, which is a bigger problem. Of course, a non-malicious client would test for invertibility of x and try again, kindly forgetting that they have accidentally cracked our RSA ring. But when N has thousands of bits, a trillion clients could do this constantly for a trillion years and none of them would ever get lucky. In what follows, we simply assume that clients generating random elements of Z_N always get invertible ones.\n\nGiven x in Z_N^*, write its logarithm as before:\n\nbeginaligned\nlog_g(x) = (a b c d)\nendaligned\n\nWe’d like to find k = tz(c), where tz is the “trailing zeros” function, sometimes also called the 2-adic valuation and denoted as nu_2, but we’ll use the more memorable function name, tz, in this writeup. We can’t actually efficiently compute logarithms in Z_N^*, however—even knowing the factorization, the size of P and Q make computing discrete logarithms in Z_P^* and Z_Q^* infeasible. Fortunately, we don’t have to explicitly compute logarithms. First, we can project onto Z_Q^* by computing fmod(x Q) in Z_Q^* which has:\n\nbeginaligned\nlog_g(fmod(xQ)) = (cd) in Z_2^m times Z_q\nendaligned\n\nThe C_q part of this can be annihilated by raising to the q power: \n\nbeginaligned\nlog_g(fmod(xQ)^q) = q(c d) = (qc 0)\nin Z_2^m times Z_q\nendaligned\n\nWe could raise this to q^-1 bmod2^m in order to undo the scaling by q, but we don’t actually need to: since q is odd, we have k = tz(qc) = tz(c) and ord(fmod(xQ)^q) = 2^k. We can efficiently find k by starting with fmod(xQ)^q and squaring it in Z_Q^* until we reach one, counting how many squarings are needed.\n\nThis gives us a computationally efficient way for someone who knows the factorization of N to turn a random, uniformly sampled element, x in Z_N^*, into a geometrically distributed sample value, k = tz(c). Crucially for our purposes, someone who doesn’t know the factorization of N cannot compute this geometric sample value.","title":"Geometric RSA rings"},{"category":"section","location":"04-hyperloglog-over-rsa/#Blurring-fingerprints","page":"HyperLogLog Over RSA","text":"We now have a construction where a client can sample a value with a geometric distribution without knowing the value they’ve sampled. We can’t, however, have a client just generate a random x in Z_N^* and send that x every time—this would be a massive, uniquely identifying client fingerprint. We need some way of destroying all identifying information about x except for the geometric sample that we want. Fortunately, this turns out to be entirely doable.\n\nWe want to erase all information about the C_2^m component except for its order, effectively preserving only tz(c). Fortunately, this is actually straightforward: for any odd t, x^t has the same geometric sample value as x. We can see this easily from logarithms:\n\nbeginaligned\nlog_g(x^t) = t (a b c d) = (ta tb tc td)\nendaligned\n\nSince  tz(tc) = tz(t) + tz(c) = tz(c) we know that x^t and x have the same geometric value. This crucially depends on t being odd. The higher bits of tc are fully random: given c and c with k = tz(c) = tz(c) we can find t that connects them, namely t = (c2^k)(c2^k)^-1 bmod2^m. Instead of sending x, the client can choose a new random, odd t for each request and send x^t. As long as t can fall into any odd residue class modulo 2^m, the leading bits of tc bmod2^m are completely arbitrary and only the position of the trailing bit is preserved.\n\nSending x^t obscures x's exact value of c, but what about the other components? The Chinese Remainder Theorem tells us that if b and d are non-zero, for any b and  d there exists t such that the following modular equalities all hold:\n\nbeginaligned\nt = 1        pmod 2 \nt = bb^-1 pmod p \nt = dd^-1 pmod q \nendaligned\n\nThis is possible since 2, p and q are pairwise coprime, and gives:\n\nbeginaligned\nlog_g(x^t) = (ta tb tc td) = (a b tc d)\nendaligned\n\nWe have ta = a bmod 2 since t is odd. In other words, we can hit any combination of values in the C_p and C_q components for some value of t in Z_pq2^m.\n\nBeing able to reach any possible values in the C_p and C_q components via exponentiation is predicated on b and d being non-zero, however. In an RSA ring with our proposed structure, if N is thousands of bits (as it would be in real usage), it is astronomically unlikely to choose x with zero exponents in the C_p or C_q components. For the sake of sheer thoroughness, however, let’s erase even that tiny bit of information. Choose z in Z_N^* at random and multiply by w = z^2^m. If log_g(z) = (a_z b_z c_z d_z), we have:\n\nbeginaligned\nlog_g(w)\n= 2^m  (a_z b_z c_z d_z) \n= (0 b_z 2^m 0 d_z 2^m) \nendaligned\n\nThe C_2 and C_2^m exponents of w are both zero since 2^m = 0 in both moduli. The C_p and C_q parts are random and arbitrary since b_z and d_z are random and arbitrary and multiplication by 2^m just permutes those already random values. Since p and q are coprime, the Chinese Remainder Theorem guarantees the existence of e in Z such that:\n\nbeginaligned\ne = b_z 2^m pmod p \ne = d_z 2^m pmod q \nendaligned\n\nThis lets us write the logarithm of w with a single unknown:\n\nbeginaligned\nlog_g(w) = (0 e 0 e)\nendaligned\n\nWe can multiply by w before or after exponentiating by t:\n\nbeginaligned\nlog_g(wx^t) = (a tb + e tc td + e) \nlog_g((wx)^t) = (a t(b + e) tc t(d + e)) \nendaligned\n\nEither way, every possible value in the C_p and C_q components can be reached for some value of w and t. We’ll use wx^t; this version has a somewhat stronger guarantee: regardless of the other values, including t, there is some value of w that can produce any pair of values in the C_p and C_q components.\n\nThe final component we need to consider is C_2: the value of a in x is unchanged by both x mapsto x^t and by multiplication by w^2^m. As it turns out, however, we actually need this parity bit to be preserved in order to prevent clients from artificially inflating their geometric samples.","title":"Blurring fingerprints"},{"category":"section","location":"04-hyperloglog-over-rsa/#Fighting-inflation","page":"HyperLogLog Over RSA","text":"Clients are supposed to raise x to an odd power, t. What happens if they raise it to an even t instead? For any values t and  c we have tz(tc) = tz(t) + tz(c). When t is odd we have tz(t) = 0, so tz(tc) = tz(c). But if t is even then tz(t)  0 and x^t inflates the geometric sample. If a client sends x^2^m, for example, then it is guaranteed to produce the maximal geometric sample value, which is supposed to be vanishingly rare, occurring with 12^m probability. If we require that a = 1 bmod 2 in x, however, then ta = t bmod 2, which lets us read the parity of t from the first component of wx^t. Parity bit to the rescue! So we’d like to require clients to choose x with odd a and then check that ta = 1 in the wx^t value that is sent. Any requests not satisfying this should be ignored for client count estimation purposes, since that indicates a malicious or malfunctioning client.\n\nReaders may wonder how clients can check whether the x that they’ve chosen has a = 1 bmod 2. After all, the whole point of working in Z_N is that people who don’t know the factorization of N can’t extract the components of x in Z_N. Someone who knows the factorization of N can, of course, check this easily:\n\nbeginaligned\na = 0 iff fmod(xP)^p = 1\nendaligned\n\nBut the client doesn’t know P and can’t check this. It is, however, possible to efficiently compute the Jacobi symbol, Jacobi_N(x), without knowing the factorization of N. For this particular ring shape, the value of the Jacobi symbol is (-1)^a + c. In general, the Jacobi symbol is the total parity of all the log-coordinates whose moduli are even. This doesn’t tell us the value of a by itself, but if Jacobi_N(x) = -1 then we know that a + c = 1 bmod 2. This isn’t exactly what we wanted, but it does give us what we need. Instead of requiring a = 1, we can require that\n\nbeginaligned\nJacobi_N(wx^t) = -1  iff \n(-1)^t(a + c) = -1  iff \nt(a + c) = 1 bmod 2  iff \nt = a + c = 1 bmod 2\nendaligned\n\nIn words, this requirement forces both t and a + c to be odd. We should also verify that conditioning on Jacobi_N(x) = -1 does not change the distribution of tz(c). Since a in 01 is uniform and independent of c, for every value of tz(c) the probability mass is split equally between the two Jacobi values:\n\nx in Z_N^* J_N^+ J_N^-\ntz(c) = 0 tfrac12 tfrac14 tfrac14\ntz(c) = 1 tfrac14 tfrac18 tfrac18\ntz(c) = 2 tfrac18 tfrac116 tfrac116\nvdots vdots vdots vdots\ntotal 1 tfrac12 tfrac12\n\nThis table uses J_N^ = setx in Z_N^* st Jacobi_N(x) =  1. Every row is split evenly between J_N^+ and J_N^-, so conditioning on Jacobi_N(x) = -1 leaves the distribution of tz(c) intact. Equivalently, tz(c) and Jacobi_N(x) are independent random variables.\n\nIn summary, instead of requiring x with a = 1 bmod 2, which would work but cannot be checked by clients, we require x with Jacobi_N(x) = -1, which they can check. This implies that Jacobi_N(wx^t) = -1 if and only if t is odd. The server can check that this is the case and disregard any requests that don’t satisfy this parity requirement.","title":"Fighting inflation"},{"category":"section","location":"04-hyperloglog-over-rsa/#Bucket-brigade","page":"HyperLogLog Over RSA","text":"We now have the ability for clients to blindly sample geometric values, but for full HyperLogLog sampling we also need a uniformly distributed bucket value. Can we modify our RSA ring to include a bucket value too? We certainly can:\n\nbeginaligned\nN = P Q = (2 B p + 1)(2^m q + 1)\nendaligned\n\nThis is the full RSA ring shape for encrypted HyperLogLog sampling. As before, P, Q, p, and q are distinct, odd primes, m is the maximum geometric sample value, and B is the number of HyperLogLog buckets, which must be odd and coprime to everything else. The multiplicative structure of a HyperLogLog RSA ring is:\n\nbeginaligned\nZ_N^* cong (C_2 times C_B times C_p) times (C_2^m times C_q)\nendaligned\n\nThe C_B component encodes the encrypted bucket value, the order of the C_2^m component encodes the geometric sample value, and the C_2 component is used to ensure that clients don’t inflate geometric samples by raising values to even powers. The C_p and C_q components are what make values hard to decode—we don’t actually care about the values in these components. Since this is starting to be a lot of components and we don’t actually care about C_p and C_q, we’ll combine these two into a single cyclic component with order pq:\n\nbeginaligned\nZ_N^* cong C_2 times C_B times C_2^m times C_pq\nendaligned\n\nNote that C_a times C_b cong C_ab only holds if a and b are coprime, which is the case here since p and q are distinct primes.\n\nThe client randomly chooses and saves a persistent x in Z_N^* value. For each new request, it randomly chooses w in (Z_N^*)^B2^m and t = 1 bmod2B and sends y = wx^t. It’s not too hard to intuitively see that the only meaningful pieces of information conveyed about x are:\n\nThe parity bit\nThe bucket value\nThe geometric sample\n\nWe can analyze it in terms of generator exponents. Choose a semigenerator, g, in this new ring and write log_g(x) = (a b c d) and log_g(w) = (0 0 0 e). Then:\n\nbeginaligned\nlog_g(wx^t)\n= (ta tb tc td + e) \n= (a b tc td + e) \nendaligned\n\nSpelling these four components out:\n\nParity bit: ta = a bmod 2 since t = 1 bmod 2\nBucket index: tb = b since t = 1 bmod B\nGeometric sample: tz(tc) = tz(t) + tz(c) = tz(c) since tz(t) = 0\nThe rest: td + e bmodpq is freshly random in each request\n\nThe core HyperLogLog Over RSA construction is now in place. The server constructs a ring with the shape N = PQ = (2Bp+1)(2^m q+1), keeping P, Q, p, q secret and publishing N together with a semigenerator g. A client generates a persistent secret x in Z_N^* with Jacobi_N(x) = -1 and, for each request, sends a freshly randomized token y = wx^t where t equiv 1 bmod2B and w is chosen from (Z_N^*)^B2^m. The server discards any request where Jacobi_N(y) neq -1, since that indicates a misbehaving client. For the remaining requests, it uses the secret factors, P and Q, to extract the bucket index, b, from the C_B component and the geometric sample, k = tz(c), from the C_2^m component, yielding a decrypted HLL sample pair, (b k). Clients cannot steer or bias samples without knowing the factorization of N. The next section completes the protocol by making it work nicely with resource class sharding.","title":"Bucket brigade"},{"category":"section","location":"04-hyperloglog-over-rsa/#Master-keys","page":"HyperLogLog Over RSA","text":"The construction so far assumes that each client chooses a single random x in J_N^- that encodes a single HLL sample. But one more ingredient is needed for the full protocol. Resource class sharding requires that each client has separate, statistically independent secrets for each one of an unbounded set of resource classes. If a client reused the same x in every resource class, it would have the same HLL value in every class. Clients must use a different, unlinkable x value for each resource class.","title":"Master keys"},{"category":"section","location":"04-hyperloglog-over-rsa/#Possible-approaches","page":"HyperLogLog Over RSA","text":"The naive solution is to store a separately generated x per class. This works but involves a distasteful amount of storage: 1KB per Z_N^* value and perhaps 5k resource classes over a user’s lifetime, so nearly a megabyte of persistent storage (per server that the client talks to, which, for most clients, is only one). Another paranoid reason not to store a secret per class: such a cache provides someone who gets access to the client’s storage a complete record of every package they’ve ever installed. Of course, the attacker can also just look at what packages they have installed, but a cache of resource-class secrets would persist even when all traces of an installed package have been removed. If we provide a way to clear or purge the cache, we are essentially refreshing the client’s identity for counting purposes, which is not catastrophic, but is undesirable.\n\nWhat we really want is a way for each client to generate a single “master key” and derive individual resource-class-specific x values from the master key and resource class. The most obvious way to do this is to hash the master key and the resource class together into Z_N:\n\nbeginaligned\nx = hash_N(textkey textclass) in Z_N^*\nendaligned\n\nHere hash_N is some cryptographic hash function taking a tuple of arguments to a uniformly distributed value in Z_N. The complication is that we need to guarantee that Jacobi_N(x) = -1 for inflation prevention. We could include a counter in the hash:\n\nbeginaligned\nx_i = hash_N(textkey textclass i)\nendaligned\n\nand use the first x_i such that Jacobi_N(x_i) = -1. Since about half of the values in Z_N have a Jacobi symbol of -1, it should only take a few attempts to find a usable x_i. This works, but is somewhat inelegant and involves computing a variable number of (large) hash values and Jacobi symbols for each counter value. Keep in mind that this work has to be done for each request the Pkg client wants to make to a server, so having it be at all expensive or non-deterministic would be best to avoid.","title":"Possible approaches"},{"category":"section","location":"04-hyperloglog-over-rsa/#What-we-actually-do","page":"HyperLogLog Over RSA","text":"Can we design a scheme where the client generates a single pre-validated master key and constructs x for each resource class so that it always has a negative Jacobi symbol? And ideally, every possible value in the negative Jacobi set would be reachable for some potential resource class. As it happens, this is possible. The key insight is that our particular shape of RSA ring, while not cyclic, is very nearly cyclic. Recall that it has this multiplicative structure:\n\nbeginaligned\nZ_N^* cong\nC_2 times C_B times C_2^m times C_pq\nendaligned\n\nThe only shared factor among cyclic component orders is a single factor of two shared by C_2 and C_2^m. This means that if g is a semigenerator, then every element in Z_N^* is either of the form g^k or x_0 g^k where x_0 is any fixed element with Jacobi_N(x_0) = -1. Recall that for a semigenerator, g in Z_N^*, both fmod(gP) and fmod(gQ) are generators in Z_P^* and Z_Q^*, respectively. All semigenerators in this ring have Jacobi_N(g) = 1, so every element of the form g^k has positive Jacobi symbol while every element of the form x_0 g^k has negative Jacobi symbol. This gives us a natural way to generate every negative Jacobi value: fix x_0 and g and let k range over all exponent values: for every x in J_N^- there is some k such that x = x_0 g^k. The client can easily pick a valid x_0 since all they have to check is that Jacobi_N(x_0) = -1. The client cannot, on the other hand, check if g is a semigenerator since it doesn’t know the factorization of N, but the server can do this and publish a common g value along with N. Clients cannot check that g is actually a semigenerator, but there’s no real harm done if it isn’t.\n\nPutting it together:\n\nThe server, when generating the ring, also chooses and publishes a common “semigenerator” element, g in Z_N^*;\nThe client, when downloading the ring parameters for the first time, also chooses and saves a random x_0 in Z_N^* with Jacobi_N(x_0) = -1. This x_0 is the client’s master key.\n\nSince half of the values in Z_N have negative Jacobi symbol, a viable x_0 is quick to find, and it only has to be done once for a new ring. Regardless of which x_0 the client chooses, every x in Z_N^* with Jacobi_N(x) = -1 has x = x_0 g^k for some k. The client’s choice of x_0 changes how exponents map to x values in a way that we’ll explore below. Write the logarithm vector of the master key as:\n\nbeginaligned\nlog(x_0) = (a b c d)\nendaligned\n\nSince Jacobi_N(x_0) = (-1)^a + c = -1 we know that a + c = 1 bmod 2. In other words, exactly one of a or c is odd. Other than this relation, the four values are completely random. For each resource class, the client derives a client- and class-specific hash value, h:\n\nbeginaligned\nh = hash(x_0 textclass)\nendaligned\n\nThis uses x_0 as salt string, rather than as a ring element; we will also use x_0 as a ring element later. Using x_0 as salt makes the hash value, h, client-specific in a highly unpredictable manner. The server cannot learn a client’s h value, but even if they did, it doesn’t reveal anything about h values in other classes. The hash function, hash, used here, is different from the previously proposed hash_N — this hash’s output is used as an exponent, rather than a ring element. This seems like a minor difference, but we’ll see that this usage requires far fewer output bits and makes our derivation significantly faster. Next, we use x_0 and h to derive the client’s personal resource class secret:\n\nbeginaligned\nx_h = x_0 g^h\nendaligned\n\nThis is the second usage of x_0, this time as a ring element. Note that the construction of x_h guarantees that it has negative Jacobi symbol:\n\nbeginaligned\nJacobi_N(x_h) = Jacobi_N(x_0) Jacobi_N(g)^h = -1\nendaligned\n\nThis x_h plays the role of x in previous sections, where we presumed it to be a random value with negative Jacobi symbol. As long as the hash values cover a sufficiently large range, x_h is an arbitrary value in J_N^-:\n\nord(g)\n= normJ_N^+\n= tfrac12normZ_N^*\n= tfrac12varphi(N)\n= 2^m B p q\n= lambda(N)\n\nIf h can take on every value in Z_lambda(N) then x_h takes on every possible value in J_N^-. Naively, this presents a problem: lambda(N) is large and unknown to the client. Fortunately, h does not actually need to cover this entire range because the client doesn’t send x_h as is: it actually sends y = wx_h^t where w in W is random “white noise” and t is a random exponent with t = 1 bmod 2B. If log_g(x_0) = (a b c d) and log_g(w) = (0 0 0 e) then we have:\n\nbeginaligned\nlog(y)\n= log(w x_h^t)\n= log(w (x_0 g^h)^t) \n= t  (a + h b + h c + h d + h) + (0 0 0 e) \n= (a + h b + h t(c + h) t(d + h) + e) \nendaligned\n\nThe components of y that convey information are:\n\nThe parity bit: a + h in Z_2\nThe bucket index: b + h in Z_B\nThe geometric sample: tz(t(c + h)) in set0 dots m\n\nHere we can see that the real requirement on h is that (b + h c + h) covers all of Z_B times Z_2^m fairly uniformly as h takes on different values. To ensure this it’s sufficient to ensure that h is sampled from a modulus, M such that fmod(M B2^m) is tiny relative to M div B2^m. We could use an exact multiple of B2^m, which makes the modulus zero. But that’s inconvenient, since real world hashes have power-of-two outputs. Fortunately, if M is sufficiently large, the bias (ratio) is negligible. For any modern hash function like SHA256, this is the case. In our reference implementation, we actually use the output of SHA256, truncated into a 128-bit integer value, which is still more than large enough to guarantee effectively uniform coverage.\n\nWhat information do we make sure is not conveyed?\n\nThe leading digits of t(c+h) should be fully random\nThe last component, t(d + h) + e, should be fully random\n\nTo ensure the former, we want t to be able to take every odd residue class in Z_2^m which is accomplished by choosing i in 0 2^m-1) and letting t = 2Bi + 1. To ensure the latter, we want w to take every possible value in W which is accomplished by choosing z in Z_N^* at random and letting w = z^B2^m (we don’t actually care about t for this factor).\n\nPutting it all together, for each request a client makes, this scheme requires the client to:\n\nCompute h = hash(x_0 textclass) — one SHA256 hash operation\nCompute g^h bmod N — one modular exponentiation\nCompute x_h = x_0 g^h bmod N — one modular multiplication\nGenerate random z in Z_N^*\nCompute w = z^B2^m — one modular exponentiation\nGenerate random i in Z_2^m-1\nCompute t = 2Bi + 1 — a few small arithmetic operations\nCompute x_h^t bmod N — one modular exponentiation\nCompute y = w x_h^t — one modular multiplication\n\nWhen generating z in Z_N^* the client technically needs to choose z in set1 dots N-1 and then also check that gcd(z N) = 1. Otherwise there is an astronomically slim chance that\n\nJacobi_N(y) = Jacobi_N(w) = Jacobi_N(z) = 0\n\nThis renders the request invalid, so the server would disregard it since it fails the Jacobi_N(y) = -1 check. This is so unlikely, however, that we will just take that chance on each request rather than spending time checking invertibility. As a practical matter, this will never happen—in the literal sense that for realistic values of N, it is so unlikely that even over centuries of trillions of clients using this protocol, chances are that none of them will ever choose z that isn’t in Z_N^*. If it does happen, the consequences are virtually nil: a single request will be ignored when computing a client count estimate that is approximate anyway. In other words, invertibility is absolutely not worth checking for here.\n\nHere’s the actual code for this in our test implementation:\n\nh = hash_resource_class(x₀, class)\nx = modmul(x₀, powermod(g, h, N), N)\nz = rand(rng, 1:N-1)\nw = powermod(z, widen(B) << m, N)\ni = rand(rng, 0:(Int64(1) << (m-1))-1)\nt = widemul(2B, i) + 1\ny = modmul(w, powermod(x, t, N), N)\n\nAs you can see, this is a very literal rendition of the steps described. On my M2 MacBook Air, for 1024-bit N, this takes around 80 microseconds; for 2048-bit N, it takes 435 microseconds, less than half a millisecond. In other words, these computations are easily fast enough to perform during each Pkg request — the network request itself will take orders of magnitude more time.","title":"What we actually do"},{"category":"section","location":"05-security-analysis/#Security-Analysis-(Proofs)","page":"Security Analysis (Proofs)","text":"This section formalizes the security properties of the protocol. The first part proves the central anonymity result: when N is fingerprint-free, two clients with the same HLL value produce indistinguishable token distributions, so the server learns nothing beyond that value. Rather than assuming the specific structure N = PQ = (2Bp+1)(2^m q+1), we work with a general characterization of fingerprint-free moduli — a condition that will also be needed when we analyze how servers can certify their modulus. We assume throughout that B is odd and m  2.\n\nThe second part analyzes the integrity side from the client’s perspective: how much can a malicious client inflate count estimates, and why is that effort linear rather than exponential? The third part turns to the server side: how can a server prove that its modulus is fingerprint-free without revealing its factorization, and what does it cost an attacker to forge such a proof?","title":"Security Analysis (Proofs)"},{"category":"section","location":"05-security-analysis/#Anonymity","page":"Security Analysis (Proofs)","text":"We build to the anonymity theorem in three steps: the algebraic definitions it relies on, the theorem and its proof, and an interpretation of what it guarantees.","title":"Anonymity"},{"category":"section","location":"05-security-analysis/#Definitions","page":"Security Analysis (Proofs)","text":"Definition. We write the “positive Jacobi subgroup” in Z_N^* as:\n\nbeginaligned\nJ_N^+\n= Jacobi_N^-1(+1)\n= set x in Z_N^* st Jacobi_N(x) = +1 \nendaligned\n\nWe’ll write the “negative Jacobi set” (it’s not a subgroup) as:\n\nbeginaligned\nJ_N^-\n= Jacobi_N^-1(-1)\n= set x in Z_N^* st Jacobi_N(x) = -1 \nendaligned\n\nSince the Jacobi symbol only takes 1 values in Z_N^*, J_N^- = Z_N^* setminus J_N^+, but it’s convenient to have shorter notation. Note that the Jacobi symbol is only well-defined for odd N, so when we talk about J_N^ it implicitly requires that N is odd.\n\nDefinition. We define the “white noise” subgroup of Z_N^* as:\n\nbeginaligned\nW_N = (Z_N^*)^B2^m\n= set z^B2^m st z in Z_N^*   J_N^+\nendaligned\n\nSince B2^m is even (m  2), every element has positive Jacobi symbol, so this is a subgroup of J_N^+. This subgroup is where we sample “noise” values to randomize the parts of x that don’t encode the HyperLogLog value. We previously described deriving individual w values from random z values; here we consider the entire group.\n\nDefinition.  We call a positive integer, N, “fingerprint-free” if it is odd and there exists a group homomorphism\n\nbeginaligned\nphi Z_N^* to C_B times C_2^m\nendaligned\n\nsuch that\n\nbeginaligned\nker(phi) cap J_N^+ = +W_N \nker(phi) cap J_N^- = -W_N \nendaligned\n\nProposition.  N is fingerprint-free if and only if N = 3 bmod 4 and there exists phi J_N^+ to C_B times C_2^m such that ker(phi) subseteq W_N.\n\nProof.  The forward direction is easy: restricting phi to J_N^+ already gives most of what is required, we only need to show that N = 3 bmod 4. It’s a standard identity that\n\nbeginaligned\nJacobi_N(-1) = (-1)^fracN-12\nendaligned\n\nwhich means that N = 3 bmod 4 iff -1 in J_N^-. Since 1 in W_N we know that -1 in -W_N subseteq J_N^- which implies that N = 3 bmod 4.\n\nThe reverse direction is harder. First, we’ll show that if phi exists with ker(phi) subseteq W_N then we can also find phi with ker(phi) = W_N. Let K = ker(phi). The existence of phi J_N^+ to C_B times C_2^m with K = ker(phi)  W_N tells us that J_N^+K is cyclic with order dividing B2^m. It also gives the following subgroup chain:\n\nbeginaligned\nK  W_N  J_N^+\nendaligned\n\nThis implies an inclusion of quotient groups:\n\nbeginaligned\nW_NK  J_N^+K\nendaligned\n\nMoreover, there is a canonical isomorphism:\n\nbeginaligned\nJ_N^+W_N cong (J_N^+K)(W_NK)\nendaligned\n\nSo J_N^+W_N is a quotient of a cyclic group with order dividing B2^m which means it must also match that description. This means there exists a homomorphism, phi J_N^+ to C_B times C_2^m with ker(phi) = W_N exactly. In what follows, we’ll just assume that we had ker(phi) = W_N in the first place.\n\nRecall that N = 3 bmod 4 implies that -1 in J_N^-. This allows us to extend phi to all of  Z_N^* by phi(x) = phi(-x) for x in J_N^-. We need to check four identities to verify that this is a homomorphism:\n\nx in J_N^-: phi(x) phi(x^-1) = phi(-x) phi(-x^-1) = phi(1) = 1\nx in J_N^- y in J_N^-: phi(x)phi(y) = phi(-x)phi(-y) = phi(xy)\nx in J_N^- y in J_N^+: phi(x)phi(y) = phi(-x)phi(y) = phi(-xy) = phi(xy)\nx in J_N^+ y in J_N^-: phi(x)phi(y) = phi(x)phi(-y) = phi(-xy) = phi(xy)\n\nWe already know that\n\nbeginaligned\nker(phi) cap J_N^+ = ker(phi _J_N^+) = W_N\nendaligned\n\nSuppose x in -W_N. Since -x in W_N subseteq ker(phi), we have phi(x) = phi(-x) = 1 so x is in the kernel of phi as well. This gives -W_N subseteq ker(phi). Now suppose x in ker(phi) cap J_N^-. Since x in J_N^- we have phi(x) = phi(-x) by definition, and x in ker(phi) means phi(x) = 1. Thus, -x in ker(phi) cap J_N^+ = W_N, so x in -W_N. This gives the other inclusion, and together we have:\n\nbeginaligned\nker(phi) cap J_N^- = -W_N \nendaligned\n\nThis shows that our extension’s kernel has the necessary intersections with W_N. square\n\nOur convention when N is fingerprint-free will be that if x in Z_N^* we’ll write barx = phi(x) in C_B times C_2^m and if barf is a function on C_B times C_2^m, we’ll write f = barfphi for the composition whose domain is Z_N^*. So you can generally think of bartriangle as the “essential version” of triangle, whether triangle is an element or a function.\n\nDefinition. We generalize our earlier definition of a “semigenerator” in a multiplicative group. Let G = prod_i=1^n C_alpha_i be a product of cyclic groups and let pi_i G to C_alpha_i be the canonical projection onto the ith component. An element g in G is called a semigenerator if the projection of g onto each cyclic component is a generator for that component:\n\nbeginaligned\nA i C_alpha_i = set pi_i(g)^k st k in Z \nendaligned\n\nIf the  alpha_i are pairwise coprime, then G is cyclic and g is a true generator. Otherwise, G is not cyclic and g cannot generate it. We can, however, express any x in G in terms of its logarithm with respect to the projection of g onto each cyclic component:\n\nbeginaligned\nlog_g(x) = (ell_1 ell_2 dots ell_n)\nin prod_i=1^n Z_alpha_i\nendaligned\n\nIn what follows, let barg in C_B times C_2^m be a fixed semigenerator.\n\nDefinition. The “essential HyperLogLog function” maps each value in C_B times C_2^m to its HyperLogLog sample value:\n\nbegingathered\nbarhll C_B times C_2^m to B times m 05em\nbarhll(barx) = (b tz(c)) 08em\ntextwherelog_barg(barx) = (b c)\nendgathered\n\nThe first value, b, implicitly depends on the choice of semigenerator, barg, whereas the latter, tz(c), does not: tz(c) only depends on the multiplicative order of the C_2^m part of barx, which is independent of barg. The higher bits of c do depend on barg, but the position of the last bit does not.\n\nThe HyperLogLog function for fingerprint-free N on Z_N^* is defined as hll = barhllphi, i.e. the composition of the phi whose existence is guaranteed by fingerprint-freeness with the essential HyperLogLog function. This depends on the choice of barg for barhll and on which particular phi is chosen. For the purposes of the main proof, we can just assume that some fixed phi is chosen and used. The choice of phi doesn’t actually introduce any more ambiguity than already introduced by the choice of barg — both choices merely permute the output bucket indices.","title":"Definitions"},{"category":"section","location":"05-security-analysis/#The-anonymity-theorem","page":"Security Analysis (Proofs)","text":"Lemma. For barx bary in C_B times C_2^m we have barhll(barx) = barhll(bary) if and only if there exists t in Z with t = 1 bmod2B such that barx^t = bary.\n\nProof.  Denote the logarithms of barx and bary as:\n\nbeginaligned\nlog_barg(barx)\n= (b_1 c_1) \nlog_barg(bary)\n= (b_2 c_2) \nendaligned\n\nSuppose barhll(barx) = barhll(bary). This gives us two equalities:\n\nbeginaligned\nb_1 = b_2 bmod B \ntz(c_1) = tz(c_2) \nendaligned\n\nThe second equality implies that there exists odd u in Z_2^m such that\n\nbeginaligned\nuc_1 = c_2  pmod2^m \nendaligned\n\nWe can apply the Chinese Remainder Theorem to find t with:\n\nbeginaligned\nt = 1  pmodB \nt = u  pmod2^m \nendaligned\n\nSince u is odd and  m  1 we know that t is odd as well. This means that t = 1 bmod2B as required. Now check that barx^t = bary:\n\nbeginaligned\nlog_barg(barx^t)\n= (t b_1 t c_1) \n= (b_1 u c_1) \n= (b_2 c_2)\n= log_barg(bary)\nendaligned\n\nThus we have found t = 1 bmod2B such that barx^t = bary.\n\nIn the other direction, suppose we have t = 1 bmod2B such that barx^t = bary. This gives us two equalities:\n\nbeginaligned\nb_1 =\nt b_1 = b_2  pmod B \nt c_1 = c_2  pmod2^m \nendaligned\n\nSince t is odd, the second equality implies that tz(c_1) = tz(c_2) which means we’ve shown that barhll(barx) = barhll(bary). square\n\nTheorem. Let N be fingerprint-free. For x y in Z_N^* with the same Jacobi symbol: hll(x) = hll(y) if and only if there exists w in W_N and t in Z with t = 1 bmod2B such that wx^t = y bmod N.\n\nProof.  Since N is fingerprint-free there exists phi Z_N^* to C_B times C_2^m with ker(phi) = W_N. The following conditions are equivalent for x and y with Jacobi_N(x) = Jacobi_N(y):\n\nhll(x) = hll(y)\nbarhll(phi(x)) = barhll(phi(y))\nE t = 1 bmod2B such that phi(x)^t = phi(y)\n E t = 1 bmod2B w in W_N  such that wx^t = y\n\nEquivalence of (1) and (2) is just the definition of hll = barhllphi. The lemma we just proved gives equivalence of (2) and (3). That leaves us to prove equivalence of (3) and (4). We will show that for any odd exponent, t, we have:\n\nbeginaligned\nphi(x)^t = phi(y) iff\nE w in W_N wx^t = y\nendaligned\n\nwhich means (3) and (4) are logically equivalent. Suppose that phi(x)^t = phi(y). Let w = y x^-t and check that w in ker(phi) cap J_N^+ = W_N:\n\nbeginaligned\nphi(w)\n= phi(y x^-t)\n= phi(y) phi(x)^-t\n= 1 \nJacobi_N(w)\n= Jacobi_N(y) Jacobi_N(x)^-t\n= 1\nendaligned\n\nThis requires Jacobi_N(x) = Jacobi_N(y) in set1 and t odd so that raising to -t doesn’t change the sign. In the other direction, suppose w in W_N subseteq ker(phi) with wx^t = y. Check the required equality:\n\nbeginaligned\nphi(y)\n= phi(wx^t)\n= phi(w) phi(x)^t\n= phi(x)^t\nendaligned\n\nThis proves equivalence of (3) and (4). square","title":"The anonymity theorem"},{"category":"section","location":"05-security-analysis/#Interpretation","page":"Security Analysis (Proofs)","text":"How does this result prove that our scheme preserves client anonymity? Suppose there are two clients with x_1 and x_2 as their respective client secrets and assume that hll(x_1) = hll(x_2). We also assume that the clients follow the protocol so that Jacobi_N(x_1) = Jacobi_N(x_2) = -1. The server receives w_1x_1^t_1 from the first client. Can the server tell that it got the message from the first client rather than the second? We can apply the theorem twice to show that it cannot. First, the theorem tells us that when the client chooses w_1 in W_N and t_1 with t_1 = 1 bmod2B, we always have:\n\nbeginaligned\nhll(w_1 x_1^t_1) = hll(x_1)\nendaligned\n\nIn other words, the value the client sends has the same hll-value as its secret. Next, since hll(w_1 x_1^t_1) = hll(x_1) = hll(x_2), the theorem tells us that there exists  w_2 in W_N and t_2 in Z with t_2 = 1 bmod2B such that\n\nbeginaligned\nw_2 x_2^t_2 = w_1 x_1^t_1\nendaligned\n\nThis means that either client could have sent the same value—the server cannot distinguish between messages from correctly behaving clients with the same hll value.\n\nThere is also the question of distribution of values: it could be possible that either client could send the same value but it’s much more likely for one of them to do so. This depends on how w and t values are chosen, which we haven’t addressed yet. Fortunately, so long as they are chosen uniformly from publicly known ranges, every possible value is equally likely for each client, so there is truly no way to distinguish between clients with the same HyperLogLog value.\n\nWith the anonymity theorem in hand, we turn to the integrity side of the protocol.","title":"Interpretation"},{"category":"section","location":"05-security-analysis/#Malicious-clients","page":"Security Analysis (Proofs)","text":"Plaintext HyperLogLog counting has a serious inflation problem: an attacker can send one request for each bucket, each carrying the maximum geometric value, and instantly push the unique client estimate to its ceiling. How does our protocol compare? Our gold standard is the naive unique client ID approach, where each forged request inflates the count by exactly one—attack effort is linear with coefficient one.\n\nIf a malicious client sends y with Jacobi_N(y)  -1, the server will detect it. So we can focus on a malicious client sending y with Jacobi_N(y) = -1. As discussed in the previous section, every element y in J_N^- is of the form y = x_0 g^h where g is our chosen semigenerator and x_0 is an arbitrary twist element with Jacobi_N(x_0) = -1. Here we use h as just an arbitrary attacker-controlled exponent value, not necessarily the output of a hash function. The question is whether a malicious client can influence y to have large geometric sample values.\n\nThe server doesn’t publish any x_0 value, so clients have to generate one for themselves. However, since they don’t know the factorization of N, they have no idea what its logarithms are. For our analysis, write log(x_0) = (a b c d), but keep in mind that the attacker has no idea what these values are. The HyperLogLog sample that y = x_0 g^h encodes is (b + h tz(c + h)). The attacker controls h but doesn’t know b or c. Since they don’t know c, they cannot force tz(c + h) to be large or know how large it is for any particular h. In order to hit k  tz(c + h) the attacker needs to happen to choose h whose last k bits complement c perfectly, which occurs with probability 12^k — exactly the probability of picking a value that good by chance. What they can do, however, is scan through consecutive h values. If they scan 2^k consecutive values, they’re guaranteed to hit h = -c bmod2^k for one of those values and they may happen to hit something better.\n\nIn the unique ID scheme, each request with a freshly “forged” client ID inflates the client count by one. How does HLL over RSA compare to this when an attacker tries to inflate its estimates? The expected maximum value of tz(c + h) is k+1 when scanning a consecutive block of 2^k values. This is slightly better than sending random values. If an attacker sends B2^k spoofed requests they can push each bucket up to an expected value of k + 1, which gives a client count estimate of:\n\nbeginaligned\nhatn = frac12 ln(2) B 2^k+1 = frac1ln(2) B2^k\nendaligned\n\nThis uses the original Flajolet et al. (2007) estimator for B  128. Better estimators exist for small samples, and should be used for client count estimates, but for this analysis the original asymptotic formula is fine. So, at scale, an attacker can expect to inflate the estimate by ln(2)^-1 approx 144 per malicious request. This is a rather good result: attack effort is linear and the coefficient is just a bit larger than one—only slightly worse than the unique ID gold standard. In short, whereas the naive unique ID scheme has perfect accuracy, resists inflation well, but has awful privacy, our HLL over RSA scheme is approximate (with tunable accuracy), recovers nearly the same inflation resistance, and provides provable client anonymity.\n\nThere is an important assumption hiding inside this linear bound: that the attacker cannot tell which of their forged tokens decoded to large geometric values. The bound holds precisely because, unable to decode, the attacker can’t cherry-pick—every forged request is a fresh blind sample, so pushing the count up takes linear effort. But the whole purpose of the system is to publish counts, and a published count is exactly the oracle the attacker otherwise lacks. Inject a token into an otherwise-sparse slice, watch whether the reported cardinality jumps, and you learn whether that token happened to decode high—without breaking any encryption. With that feedback an attacker can curate a collection of high-k tokens, one per bucket, and a curated set is no longer linear: roughly B requests can then push every bucket to level k, inflating the estimate to sim frac1ln 2 B 2^k, the same exponential leverage as the plaintext scheme. So the linear-resistance result holds specifically against an attacker who cannot observe per-slice counts. The mitigation is the minimum-cardinality floor introduced for anonymity (Resource class sharding and the Protocol Summary): if sub-threshold slices are never reported, the attacker has no sparse slice to probe and no decoding oracle to build. Pleasingly, the same mechanism that protects anonymity in small populations also closes this inflation channel—it is one more reason to enforce the floor, not a separate defense.\n\nIt’s worth flagging a stronger mitigation that we have deliberately not adopted, since it’s a natural question. One could try to bind each token to its request—mixing something request-specific (a server-issued nonce, or the full request path) into the value the client sends—so that a single high-value forgery can’t be reused across many requests. The difficulty is that honest counting needs the opposite: a client’s value must be stable across its requests within a class, or we’d be counting requests instead of unique clients. A binding that preserved per-(client, class) stability while denying an attacker reuse of a curated value is not obviously achievable, and in any case the cardinality floor already removes the count oracle that the curated-set attack depends on. So we rely on the floor and leave token-request binding as an open question—to revisit only if some deployment leaks count feedback through a channel the floor can’t cover.\n\nWhile this is quite a good result, our analysis of resistance to malicious clients is somewhat weak. If an attacker can learn the c coordinate of any x_0 value with respect to any semigenerator, g, then they can forge arbitrarily rare x_0 g^h values by choosing h = -c bmod 2^k for whatever k they want. They can also cover all the buckets by covering all h bmod B residue classes. A proof of attack resistance would use knowledge of such a tuple, (x_0 g c), to factor N or perform some other computation that is believed to be hard in RSA rings. We do not have such a proof. The best we have is an argument that this seems hard since the exponent coordinates in Z_N^* are secret in a way that many protocols rely on, and deciphering even one is widely believed to be hard. Without the requirement that Jacobi_N(x_0) = -1 it would be quite easy to find values with known coordinates with respect to g: just take powers of g. Since Jacobi_N(g) = 1, however, any power of g also has positive Jacobi symbol. To get to a value with negative Jacobi symbol, one needs an element with negative Jacobi symbol, which presents would-be attackers with a seemingly unsolvable bootstrapping problem. There’s no apparent way to make that leap without introducing an unknown c coordinate.\n\nThis is a vaguer argument than I would like to make in a write up full of rigorous proofs. Fortunately, proving that client anonymity is preserved is the much more important result, and for that we do have a solid proof. Inability to provably guarantee resistance to malicious client attacks is more acceptable: that’s a risk that we, as the operators of the system, can choose to take on. If our estimates suddenly start looking ridiculously large, we can begin to wonder if someone has cracked the protocol.","title":"Malicious clients"},{"category":"section","location":"05-security-analysis/#Malicious-servers","page":"Security Analysis (Proofs)","text":"So far we’ve worried about malicious clients but have assumed that servers behave as intended. Servers are supposed to generate N with the following arithmetic structure:\n\nbeginaligned\nN = P Q = (2 B p + 1)(2^m q + 1)\nendaligned\n\nwhere B and m are published parameters and P, Q, p and q are secret primes. If a client knew P and Q they could check the construction of N, but the whole point, of course, is that they don’t know the factorization. Otherwise they could easily forge rare HyperLogLog values. What if a server generates N with a different structure? Can that allow them to “fingerprint” and track individual clients? As it turns out, constructing N with different structure than intended can allow a server to fingerprint clients. Because of this, our protocol needs a mechanism for servers to convince clients that they have not hidden any fingerprints in the construction of N. This only needs to be checked when a client talks to a server initially and downloads new protocol parameters (or when the server issues new protocol parameters).","title":"Malicious servers"},{"category":"section","location":"05-security-analysis/#The-fingerprinting-threat","page":"Security Analysis (Proofs)","text":"First, let’s get a feel for how a malicious server could fingerprint clients. Here’s an alternative structure that would allow a server to uniquely identify clients:\n\nbeginaligned\nN = P Q = (2^m B p + 1)(2^m B q + 1)\nendaligned\n\nIn other words, P-1 is divisible by 2^m instead of just 2 and Q-1 is divisible by B when it shouldn’t be. With this alternative structure, Z_N^* has the following richer multiplicative structure:\n\nbeginaligned\nZ_N^* cong\nC_2^m times C_B times C_p times\nC_2^m times C_B times C_q\nendaligned\n\nWhen the client follows the protocol and picks a random x_0 in J_N^-, it has two random C_B components and two random C_2^m components instead of one of each. How much extra identifying information about each client does the malicious server get from this?\n\nFirst, consider the two C_B components. When the client raises their x value to t = 1 bmod2B both C_B components are preserved. Instead of getting log_2(B) bits of identifying information from them, the server gets 2 log_2(B) bits. When B approx 2^12 that’s 24 bits of fingerprint. Multiplying x^t by a white noise value, w in W = (Z_N^*)^B2^m, also doesn’t touch either of the two C_B and C_2^m components—the definition of W_N is specifically crafted to leave those components intact while randomizing everything else.\n\nNext, consider the two C_2^m components. When there is only one C_2^m component, taking x^t with random odd t destroys all information in it except for the position of the last bit, which is only log_2(m) bits of information. When there are two C_2^m components, however, the information provided doesn’t just double: since the two C_2^m components are scaled in lock-step, their ratio remains fixed, which carries significantly more information. It’s not hard to see that two C_2^m components raised to random t, convey a full m bits of client fingerprint. Again, multiplying by w doesn’t affect this, by design. So the malicious server gets an additional m bits of fingerprint from the two C_2^m components.\n\nWith this structure of N then, the malicious server gets a total of 2 log_2(B) + m bits of client fingerprint. For our usual choice of B and m, that’s 2cdot12 + 63 = 87 bits, which is enough to uniquely identify billions of clients with random fingerprints without significant chance of collisions. If we left this unmitigated, it would be a serious defect in our protocol.\n\nCan we convince a client that we aren’t smuggling fingerprint bits in the structure of N without giving away its factorization? In this particular case, the unusual structure of N is actually quite easy to detect:\n\nThe correct structure has\nP = 3 bmod 4 and Q = 1 bmod 4 implies N = PQ = 3 bmod 4\nP = 1 bmod B and Q  1 bmod B implies N = PQ  1 bmod B\nThis incorrect structure has\nN = P = Q = 1 bmod 4\nN = P = Q = 1 bmod B\n\nUnfortunately, not all possible malicious structures are so easy to detect. For example, if N = PQR where P = Q = 1 bmod 4 and P = Q = 1 bmod B and R = 3 bmod 4 and R  1 bmod B, then you’d have N = PQR = 3 bmod 4 and N = PQR  1 bmod B so N looks normal from simple modular criteria, yet PQ carries the 2 log_2(B) + m bits of client fingerprint from before. To guarantee that a server cannot fingerprint clients, more evidence about the structure of N needs to be provided.\n\nZero-knowledge proofs (ZKPs) are a popular solution to this kind of problem. I spent a good bit of time going down this rabbit hole. It’s definitely doable. However, every time I sat down to implement zero-knowledge proofs of semiprimality, I found myself getting bogged down in complex and fussy details. This isn’t just a matter of laziness—if the code is that hard to implement, I find it hard to convince myself that it’s fully correct. And if we’re not confident in the correctness of the code that checks whether N has the right structure, then we haven’t really proven anything.\n\nI found myself really wishing for a simpler way to demonstrate the structure of Z_N^*. One that would be easier to have confidence in, both in the sense of having a simpler implementation to review, but also in the sense of having mathematics that’s easier for more people (including myself) to understand. After some research, I found some straightforward evidence that the server can provide to convince clients a server-provided modulus cannot be used to fingerprint clients.","title":"The fingerprinting threat"},{"category":"section","location":"05-security-analysis/#Criteria-for-fingerprint-freedom","page":"Security Analysis (Proofs)","text":"Recall from the section with the formal anonymity proof that an odd integer, N, is shown to be fingerprint-free if N = 3 bmod 4 and J_N^+W_N is cyclic with order dividing B2^m. These subgroups of Z_N^* have the following definitions:\n\nbeginaligned\nJ_N^+\n= set x in Z_N^* st Jacobi_N(x) = 1 \n05em\nW_N\n= (Z_N^*)^B2^m\n= set x^B2^m st x in Z_N^* \nendaligned\n\nOur proof in that section justified the term “fingerprint-free” by showing that as long as N satisfies this property, a server cannot distinguish between two correctly behaving clients with the same HyperLogLog values. Our task then, is to come up with a way for the server to convince clients that the value of N it sends them is actually fingerprint-free, without revealing its factorization.\n\nWe first present a few supporting results about the arithmetic structure of N. For all of the following results, we assume that N is positive and odd with prime factorization given by:\n\nbeginaligned\nN = prod_i=1^D p_i^n_i\nendaligned\n\nwhere p_i are distinct odd primes and n_i  1. This prime factorization is, of course, uniquely determined by N. The ring structure of Z_N is given by:\n\nbeginaligned\nZ_N cong prod_i=1^D Z_p_i^n_i\nendaligned\n\nThe multiplicative structure of each component is:\n\nbeginaligned\nZ_p_i^n_i^* cong C_(p_i-1)p_i^n_i-1\nendaligned\n\nLet g be a semigenerator for Z_N^* so we can write the logarithm of a generic element x in Z_N^* as:\n\nbeginaligned\nlog_g(x) = (ell_1 ell_2 dots ell_D)\nin prod_i=1^D Z_(p_i-1)  p_i^n_i-1\nendaligned\n\nWe will often prove results in terms of logarithmic coordinates. These kinds of results are not usually proven with this approach, but since we’ve already worked extensively with this viewpoint, it will hopefully be more illuminating in this context.\n\nFact.  gcd_i(p_i-1) divides gcd(N-1).\n\nProof.  Let d = gcd_i(p_i-1). Since d divides p_i-1 we have p_i = 1 bmod d, which implies:\n\nbeginaligned\nN\n= prod_i p_i^n_i\n= prod_i 1^n_i\n= 1 bmodd\nendaligned\n\nThis means that d divides N-1 as required. square\n\nDefinition (standard). An element x in Z_N is called a “quadratic residue” if there exists r in Z_N such that r^2 = x bmod N. The set of quadratic residues may be denoted as (Z_N)^2.\n\nLemma. Let N be an odd, positive integer. N has at most two distinct prime factors if and only if for all setx y subseteq J_N^+ at least one of setx y xy is a quadratic residue.\n\nProof.  Since (p_i-1)p_i^n_i-1 is always even, each of the log-coordinates has a well-defined notion of parity. We can understand both the Jacobi symbol and quadratic residues in terms of parity of log-coordinates:\n\nJacobi_N(x) = 1 iff an even number of the ell_i are odd\nx is a quadratic residue iff none of the ell_i are odd\n\nIn the D = 1 case, Jacobi_N(x) = 1 if and only if x is a quadratic residue, so all three of x, y and xy are quadratic residues. In the D = 2 case, let’s write coordinates for x, y and xy:\n\nbeginaligned\nlog_g(x) = (a_x b_x) \nlog_g(y) = (a_y b_y) \nlog_g(xy) = (a_x + a_y b_x + b_y) \nendaligned\n\nSince Jacobi_N(x) = Jacobi_N(y) = 1 we know that\n\nbeginaligned\na_x = b_x  pmod 2 \na_y = b_y  pmod 2 \na_x + a_y = b_x + b_y  pmod 2 \nendaligned\n\nOne of the following has to hold:\n\nbeginaligned\na_x = b_x = 0  pmod 2 \na_y = b_y = 0  pmod 2 \na_x + a_y = b_x + b_y = 0  pmod 2 \nendaligned\n\nEach condition means one of x, y or xy is a quadratic residue. In the D = 3 case, we have a counterexample:\n\nbeginaligned\nlog_g(x) = (1 1 0) \nlog_g(y) = (0 1 1) \nlog_g(xy) = (1 2 1) \nendaligned\n\nEach value has positive Jacobi symbol, yet none of them is a quadratic residue. The same essential counterexample works for D  3 as well, letting trailing coordinates be zeros. square\n\nCombining these results, we get the following claim.\n\nClaim. If the following conditions are satisfied:\n\nN = 3 bmod 4\ngcd(B N) = gcd(B N-1) = 1\nFor all x y in J_N^+ one of setx y xy is a quadratic residue\n\nthen N is fingerprint-free.\n\nProof.  From the prior lemma we know that the last condition is equivalent to N having at most two distinct prime factors.\n\nFirst we’ll consider the case of a single prime factor: N = P^j where P is an odd prime and  j  1 . If P = 1 bmod 4 then we’d have N = 1 bmod 4 as well, so we know that P = 3 bmod 4. Let d = gcd(B P-1). Since d divides P-1 we have d divides N-1 and therefore d divides gcd(B N-1) = 1. So B and P-1 must be coprime. Thus, the structure of Z_N^* is:\n\nbeginaligned\nZ_N^* cong C_2 times C_U\ntextwhere\nU = fracP-12  P^j-1\nendaligned\n\nWe know that U is odd and coprime to B since (P-1)2 and P are. To prove that N is fingerprint-free we need a homomorphism from J_N^+ to a subset of C_B times C_2^m whose kernel is a subset of W_N  J_N^+. In this case, however, W_N = J_N^+ so the constant homomorphism, which has all of J_N^+ as its kernel, works. To see this, consider generic x in Z_N^*:\n\nbeginaligned\nlog_g(x) = (a b) in Z_2 times Z_U\nendaligned\n\nJacobi_N(x) = (-1)^a since U is odd, so x in J_N^+ if and only if a = 0. On the other hand, w in W if and only if w = z^B2^m for some z in Z_N^* or:\n\nbeginaligned\nlog_g(w)\n= (a_w b_w)\n= (a_z B2^m b_z B2^m)\n= B2^m log_2(z)\nendaligned\n\nThis forces a_w = 0 since m  2. Does it impose any restriction on b_w? No, since for any b_w we can let\n\nbeginaligned\nb_z = b_w(B2^m)^-1 pmod U\nendaligned\n\nThis inverse exists since B2^m is coprime to U. This shows that both J_N^+ and W_N are precisely the subset where a = 0 bmod 2 and that they are equal, and therefore the constant homomorphism witnesses that N is fingerprint-free.\n\nNext, consider the case of two distinct prime factors: N = P^j Q^k where P and Q are distinct odd primes and j  1 and k  1. Since N = 3 bmod 4, we can assume without loss of generality that P^j = P = 3 bmod 4, which implies that 1 = tz(P-1). Let n = tz(Q-1). Then the structure of Z_N^* is:\n\nbegingathered\nZ_N^* cong C_2 times C_U times C_2^n times C_V 05em\ntextwhere \nU = fracP-12  P^j-1\nhspace4em\nV = fracQ-12^n  Q^k-1\nendgathered\n\nU and V are both odd and coprime to B. They are odd since P, Q, (P-1)2 and (Q-1)2^n are all odd. We know that P and Q are coprime to B since gcd(B PQ) = 1. Let d = gcd(B P-1 Q-1). Since d divides both P-1 and Q-1 it must also divide N-1, but since gcd(B N-1) = 1 the only option is d = 1. Thus, U and V are also coprime to B.\n\nLet B_U = gcd(B U) and B_V = gcd(B V). These are coprime since gcd(B U V) = 1. Let barm = min(m n)  1 and pick a semigenerator barg in C_B_U times C_2^barm times C_B_V. For x in Z_N^* with log_g(x) = (a b c d) define\n\nbeginaligned\nphi Z_N^* to\nC_B_U times C_2^barm times\nC_B_V\nendaligned\n\nbeginaligned\nlog_barg(phi(x)) = (b c d)\nin Z_B_U\ntimes Z_2^barm\ntimes Z_B_V\nendaligned\n\nNote that in addition to dropping the first coordinate, the remaining three coordinates are modularly reduced. In order to show that phi witnesses that N is fingerprint-free, we need to show that ker(phi_J_N^+) subseteq W_N. Let\n\nbeginaligned\nw in ker(phi_J_N^+) = J_N^+ cap ker(phi)\nendaligned\n\nSince w in J_N^+ we have:\n\nbeginaligned\na = c pmod 2 \nendaligned\n\nSince w in ker(phi) we have:\n\nbeginaligned\nb = 0 pmodB_U \nc = 0 pmod2^barm \nd = 0 pmodB_V \nendaligned\n\nThus, there exist b, c and d such that:\n\nbeginaligned\nb = b B_U \nc = c 2^barm \nd = d B_V\nendaligned\n\nTo show that w in W we need to find i in Z such that:\n\nbeginaligned\ni B 2^m = 0  pmod 2 \ni B 2^m = b = b B_U  pmod U \ni B 2^m = c = c 2^barm  pmod2^n \ni B 2^m = d = d B_V  pmod V \nendaligned\n\nThe first equation is automatically satisfied since m  1. The other three equations are equivalent to:\n\nbeginaligned\ni (BB_U) 2^m = b  pmodUB_U \ni B 2^m-barm = c  pmod2^n-barm \ni (BB_V) 2^m = d  pmodVB_V \nendaligned\n\nHere we have divided common factors—B_U, 2^barm and B_V, respectively—out of each equation and modulus. These equations are in turn equivalent to:\n\nbeginaligned\ni = b (BB_U 2^m)^-1  pmodUB_U \ni = c B^-1      pmod2^n-barm \ni = d (BB_V 2^m)^-1  pmodVB_V \nendaligned\n\nThe middle equation looks like it cancels a 2^m-barm that is not invertible modulo a power of two, but barm = min(m n) rules out the problematic case: either m  n, so 2^m-barm = 1 and only the odd factor B is inverted, or m  n, so the modulus 2^n-barm = 1 and the congruence is vacuous. This set of equations can be solved via the Chinese Remainder Theorem since the moduli are pairwise coprime. Let x = g^i, which gives:\n\nbeginaligned\nlog_g(x^B2^m)\n= log_g(g^iB2^m) \n= (iB2^m iB2^m iB2^m iB2^m) \n= (0 b B_U c 2^barm d B_V) \n= (0 b c d) \n= log_g(w)\nendaligned\n\nThis shows that w in W_N as claimed, so ker(phi_J_N^+) subseteq W_N and thus phi witnesses that N is fingerprint-free in the two prime factor case. square\n\nThis gives us a concrete set of criteria on N, which, taken together, imply that N is fingerprint-free. Of course, the obvious question is how can a client be convinced that for every pair setx y subseteq J_N^+ one of setx y xy has a square root? This obviously cannot be checked exhaustively by client or server, since J_N^+ is huge for realistic N. The next section gives results that allow us to design a protocol that lets a server convince clients that  N is overwhelmingly likely to be fingerprint-free.","title":"Criteria for fingerprint-freedom"},{"category":"section","location":"05-security-analysis/#Upper-bound-on-quadratic-residues","page":"Security Analysis (Proofs)","text":"The server cannot provide a square root for one of setx y xy for every pair setx y subseteq J_N^+. But if the client can challenge the server to produce square roots for arbitrary pairs, then the server can interactively convince the client that either every pair has this property or the server has gotten very lucky. And if the client can challenge the server to provide a square root for as many pairs as it wants, then it can require the server to be implausibly lucky. Moreover, an interactive protocol like this can be converted into non-interactive certificates in a standard way by using cryptographic hashing to produce random elements instead of having the client choose them. In order to know how many challenges a server should be required to answer, however, we need an upper bound on the fraction of pairs that have square roots when N has incorrect structure. The following theorem provides this upper bound.\n\nTheorem. Let N be an odd, positive integer. If N has  D distinct prime factors, then the fraction of pairs  setx y subseteq J_N^+  with at least one of setx y xy being a quadratic residue is\n\nbeginaligned\nfrac32^D-1 - frac22^2(D-1)\nendaligned\n\nIn particular, for D  3 it is at most 58.\n\nProof.  It’s straightforward to see that the probability of x in J_N^+ being a quadratic residue is 12^D-1:\n\nThere are D even log-coordinates in log_g(x) = (ell_1 ell_2 dots ell_D) which gives D independent parity bits in Z_N^*\nRequiring x in J_N^+ forces sum_i ell_i = 0 bmod 2, removing a single independent parity bit, leaving D-1 parity bits\nx is a quadratic residue if and only if all the parity bits are even, which has a probability in J_N^+ of 12^D-1\n\nNow, define three events:\n\nX: x is a quadratic residue\nY: y is a quadratic residue\nZ: xy is a quadratic residue\n\nNote that any two of these events are independent, but any two of them imply the third. Thus, we have:\n\nbeginaligned\nPr(X cap Y cap Z)\n= Pr(X cap Y) \n= Pr(X cap Z) \n= Pr(Y cap Z)\n= 12^2(D-1)\nendaligned\n\nApply the inclusion-exclusion principle to get:\n\nbeginaligned\nPr(X cup Y cup Z)\n= Pr(X) + Pr(Y) + Pr(Z)\n-Pr(X cap Y) - Pr(X cap Z) - Pr(Y cap Z) \n+Pr(X cap Y cap Z) \n= frac32^D-1 - frac22^2(D-1)\nendaligned\n\nReassuringly, for D  2 this is one—anything else would contradict our prior theorem. For D = 3 it is 58 and it decreases as D increases, so 58 is the maximum for odd N with more than two prime factors. square\n\nThis bound allows a protocol whereby a server can convince a client that N is fingerprint-free by providing enough quadratic residues.","title":"Upper bound on quadratic residues"},{"category":"section","location":"05-security-analysis/#Certifying-a-good-modulus","page":"Security Analysis (Proofs)","text":"Based on these results, we can design a protocol for a server to convince clients that N is fingerprint-free. First, the client checks that N = 3 bmod 4, that gcd(B N) = 1, and that gcd(B N-1) = 1. These are simple numerical checks. The client is then ready to be convinced that N has at most two prime divisors. The interactive version is:\n\nThe client picks n random pairs setx y subseteq J_N^+ and challenges the server to produce r in Z_N^* for each pair such that r^2 in setx y xy bmod N.\n\nThis convinces the client that there’s at most a (58)^n probability that N has more than two distinct prime factors. If the client wants to be convinced to a probability of nfrac1alpha for some large alpha, they should choose n such that:\n\nbeginalignedat2\nleft(frac58right)^n  frac1alpha\niff\nn  fraclog_2(alpha)log_2(8)-log_2(5)\nendalignedat\n\nWe can turn this into a non-interactive protocol by picking a cryptographic hashing scheme to generate pairs of values in such a way that the server cannot influence them. The hash should include N as an input so that the set of hash-generated values is not fixed and must be computed after choosing a candidate modulus. This prevents generating n pairs and then solving for an N that happens to have quadratic residues for those pairs. In order to generate an incorrectly structured N that passes this test, a malicious server would have to try about alpha candidate N values before expecting to find one that passes. Since n grows only as log_2(alpha), we can demand an enormous amount of attacker work while keeping the certificate small—but we have to choose alpha with the real cost of a forgery attempt in mind, and here it pays to be pessimistic, because a successful forgery is catastrophic. The fingerprinting structures from the start of this section yield on the order of 2log_2(B) + m approx 87 bits of fingerprint—enough to deanonymize every client of the system at once—so alpha is guarding against total failure, not a marginal leak.\n\nIt’s tempting to reason about the per-attempt cost in terms of the n quadratic-residue checks, but that reasoning cuts the wrong way: the attacker is forging their own candidate moduli, so they know each candidate’s factorization, which makes the QR checks essentially free for them. Worse for the defender, the attacker fails fast—each challenge pair passes a bad modulus with probability at most 58, so on a doomed candidate they bail after two or three pairs and pay the full n square roots only on the eventual success. The real per-attempt cost is dominated by generating a constrained candidate modulus, and the search is embarrassingly parallel. We should not lean on QR-testing being expensive; for this attacker, it isn’t.\n\nTo put numbers on it, take a deliberately attacker-favorable estimate of 10^11 modular exponentiations per second of aggregate compute, and pretend each attempt costs only a single modexp (generating a constrained prime is much more expensive, so this understates the attacker’s work, which is the safe direction for a security margin):\n\nalpha candidates wall-clock n cert size\n2^50 11times10^15 ~3 hours 74 ~9.5 KB\n2^64 18times10^19 ~6 years 95 ~12 KB\n2^80 12times10^24 ~4times10^5 years 118 ~15 KB\n2^112 infeasible infeasible 166 ~21 KB\n2^128 infeasible infeasible 189 ~24 KB\n\nBecause n grows only logarithmically in alpha, buying a gigantic safety margin is nearly free: the certificate stays a few dozen kilobytes and the server’s square-root computation stays sub-second. The recommendation is therefore to set alpha ge 2^lambda for the system’s overall security level lambda, and never below 2^80. The simplest safe choice is alpha = 2^128, which makes finding a malicious N that passes the test so unlikely that it is no longer a concern.\n\nThe non-interactive version of this protocol serves as a certificate of fingerprint-freedom for a published N value. The certificate structure contains:\n\nB — the number of buckets\nm — the maximum geometric sample value\nN — the ring modulus\ng — a server-selected semigenerator for Z_N^*\ntextsqrts — a list of square roots\n\nWhen downloading a new ring structure, a client checks the following requirements based on the data in this certificate:\n\nN = 3 bmod 4\ngcd(B N) = 1\ngcd(B N-1) = 1\nThat enough square roots are provided\nThat all the square roots are valid\n\nThat’s it. Once the client has done this, it can safely use N and proceed with the protocol for generating and sending y = xw^t values with each request, confident that it has provable anonymity.","title":"Certifying a good modulus"},{"category":"section","location":"#HyperLogLog-Over-RSA","page":"Home","text":"HyperLogLog Over RSA is a new protocol for estimating the number of unique clients making requests to a service — for instance, the number of distinct installs hitting an open-source project’s package servers — without anyone, even someone with full access to the server logs, being able to identify or track individual clients. It combines HyperLogLog cardinality estimation with the algebraic structure of RSA rings, so that clients sample encrypted HyperLogLog values they cannot themselves decode or bias.\n\nThis site includes:\n\nA writeup — a from-scratch explanation of the motivation, design, and security arguments behind it;\nA reference implementation — the HyperLogLogOverRSA.jl Julia package, a working test/demo implementation of the protocol.","title":"HyperLogLog Over RSA"},{"category":"section","location":"#Reading-the-writeup","page":"Home","text":"For the design and the privacy model:\n\nExecutive Summary — the short version;\nFor Cryptographers — a condensed, notation-first tour for readers who want to check the math;\nThe full writeup starts here and proceeds in short chapters.","title":"Reading the writeup"},{"category":"section","location":"#Using-the-package","page":"Home","text":"For running the protocol:\n\nReference Implementation — installation, a usage example, and the full API.\n\nusing HyperLogLogOverRSA\nring   = Ring(2^12-1, 63, 1024)                # generate a HyperLogLog RSA ring\ncert   = RingCert(ring)                        # certificate that the ring is fingerprint-free\nclient = Client(cert)                          # verify cert, pick a random secret\ny      = hll_generate(client, \"/package/123\")  # encrypted HLL token for a request\nhll_decode(ring, y)                            # server-side: (bucket, geometric) HLL value\n\nnote: Note\nThe implementation is a test/demo of the protocol — intended to make the design concrete and checkable, not a hardened production library.","title":"Using the package"},{"category":"section","location":"reference/#Reference-Implementation","page":"Reference Implementation","text":"The HyperLogLogOverRSA.jl package is a test/demo implementation of the full protocol described in the writeup. It exists to make the design concrete and checkable — not to be a hardened production library.","title":"Reference Implementation"},{"category":"section","location":"reference/#Installation","page":"Reference Implementation","text":"pkg> add https://github.com/StefanKarpinski/HyperLogLogOverRSA.jl","title":"Installation"},{"category":"section","location":"reference/#Usage","page":"Reference Implementation","text":"julia> using HyperLogLogOverRSA\n\njulia> ring = Ring(2^12-1, 63, 1024)   # generate a HyperLogLog RSA ring\nRing(B=4095, m=63, N=…)\n\njulia> cert = RingCert(ring)           # certificate that the ring is fingerprint-free\nRingCert(B=4095, m=63, N=…)\n\njulia> client = Client(cert)           # check the certificate, pick a random secret x₀\nClient(B=4095, m=63, N=…, x₀=…)\n\njulia> y = hll_generate(client)        # encrypted HLL token, default resource class\n…\n\njulia> hll_decode(ring, y)             # ring holder decodes the (bucket, geometric) value\n(1342, 0)\n\njulia> y = hll_generate(client)        # a fresh, unlinkable token for the same client/class\n…\n\njulia> hll_decode(ring, y)             # …decodes to the same HLL value\n(1342, 0)\n\njulia> y = hll_generate(client, \"/package/123\")   # an independent value per resource class\n…\n\njulia> hll_decode(ring, y)\n(1902, 3)\n\nThe server should log the y values and only decode them later, offline — the factorization of N never needs to live on the public-facing servers. See the Protocol Summary for the end-to-end flow.\n\nhll_estimate decodes a batch of logged tokens for one resource class and estimates the number of unique clients behind them (Ertl’s improved HyperLogLog estimator). Repeated tokens from the same client collapse, since they all decode to that client’s single value for the class:\n\njulia> ys = [hll_generate(client, \"/registries\") for _ in 1:1000];  # 1 client, many requests\n\njulia> hll_estimate(ring, ys)                                       # ≈ 1 unique client\n1.0...\n\n(One client estimates to ≈ 1 no matter how many requests it sends; a real estimate aggregates tokens from many distinct clients.)","title":"Usage"},{"category":"section","location":"reference/#API","page":"Reference Implementation","text":"","title":"API"},{"category":"type","location":"reference/#HyperLogLogOverRSA.Ring","page":"Reference Implementation","text":"Ring(B, m, L; rng=…) -> Ring\n\nA HyperLogLog RSA ring: a modulus N together with the secret structure that lets its holder decode HyperLogLog values while no one else can.\n\nThe ring has the form\n\nN = P Q = (2 B p + 1)(2^m q + 1)\n\nso that, multiplicatively,\n\nℤ_N^* ≅ C_2 × C_B × C_(2^m) × C_(p q)\n\nwhere B (which must be odd) is the number of HyperLogLog buckets and m is the maximum geometric sample value; L is the target bit-length of N.\n\nThe primes P, Q, p, q are secret — and are deliberately omitted when a Ring is shown — so only a holder of the Ring can decode HLL values via hll_decode. Publish a RingCert so that clients can use the ring without learning its factorization.\n\nrng (any AbstractRNG, default a RandomDevice) is the source of randomness for choosing the primes; pass a seeded RNG for a reproducible ring.\n\n\n\n\n\n","title":"HyperLogLogOverRSA.Ring"},{"category":"type","location":"reference/#HyperLogLogOverRSA.RingCert","page":"Reference Implementation","text":"RingCert(ring::Ring; rng=…) -> RingCert\n\nA publishable certificate for a Ring: the public parameters B, m, N, a semigenerator g, and a list of square roots that together let anyone verify N is \"fingerprint-free\" — that it has the intended two-prime structure and so cannot be used to fingerprint clients — without revealing its factorization. A Client checks this certificate before trusting a ring.\n\nrng (default a RandomDevice) is the source of randomness for picking the semigenerator g; the square roots are derived deterministically by hashing.\n\n\n\n\n\n","title":"HyperLogLogOverRSA.RingCert"},{"category":"type","location":"reference/#HyperLogLogOverRSA.Client","page":"Reference Implementation","text":"Client(cert::RingCert; rng=…) -> Client\n\nVerify a published RingCert and, if every check passes, construct a client holding the public ring parameters together with a freshly chosen random secret x₀ (a Jacobi \"twist\" element). Throws an ArgumentError if the certificate fails any check. rng (default a RandomDevice) is the source of randomness for x₀; pass a seeded RNG to get a reproducible client.\n\nCall hll_generate to produce the encrypted HyperLogLog token to send with a request.\n\nIn this reference implementation the Client object is the client's persisted identity: each instance holds its own random x₀, so two instances are two distinct clients. Persisting a client across sessions would just mean serializing the object, which is out of scope here.\n\n\n\n\n\n","title":"HyperLogLogOverRSA.Client"},{"category":"function","location":"reference/#HyperLogLogOverRSA.hll_generate","page":"Reference Implementation","text":"hll_generate(client::Client, class=\"/registries\"; rng=…) -> Integer\n\nProduce a fresh, randomized encrypted HyperLogLog token y = w xᵗ for the given resource class, to send along with a request. Every call re-randomizes the token, so two tokens from the same client are unlinkable; yet they all decode (by the ring holder) to that client's single, stable HLL value for the class. The client cannot itself decode or bias the value it samples.\n\nrng (default a RandomDevice) supplies the per-token randomness. It does not affect the decoded value — only the token's unlinkable encoding — so seeding it makes token output reproducible without changing what the token decodes to.\n\n\n\n\n\n","title":"HyperLogLogOverRSA.hll_generate"},{"category":"function","location":"reference/#HyperLogLogOverRSA.hll_decode","page":"Reference Implementation","text":"hll_decode(ring::Ring, y; bmap=bucket_map(ring)) -> (bucket, geometric)\n\nDecode an encrypted HyperLogLog token y to its (bucket, geometric) value, using the secret factorization carried by ring. Pass a precomputed bmap (from bucket_map) to amortize bucket decoding across many tokens.\n\n\n\n\n\n","title":"HyperLogLogOverRSA.hll_decode"},{"category":"function","location":"reference/#HyperLogLogOverRSA.hll_estimate","page":"Reference Implementation","text":"hll_estimate(ring::Ring, tokens) -> Float64\n\nEstimate the number of unique clients behind a collection of encrypted HLL tokens — the y values logged for a single resource class. Each token is decoded with ring (sharing one bucket_map across the batch) and aggregated into a HyperLogLog sketch; repeated tokens from the same client collapse automatically, since they all decode to that client's single (bucket, geometric) value for the class.\n\nUses the improved estimator of Ertl (2017): unbiased across the whole cardinality range, with relative standard error ≈ 1.04/√B.\n\n\n\n\n\n","title":"HyperLogLogOverRSA.hll_estimate"},{"category":"function","location":"reference/#HyperLogLogOverRSA.bucket_map","page":"Reference Implementation","text":"bucket_map(ring::Ring) -> Dict\n\nPrecompute the map from C_B representatives in ℤ_P^* to bucket indices 0:B-1, so that repeated hll_decode calls need not recompute it.\n\n\n\n\n\n","title":"HyperLogLogOverRSA.bucket_map"}]
}
